# Context - Lamott Allen Compatibility Alias

This directory exists so `execution/validate_skill.py lamott-allen-really-real-writing` can find the expected agent slug.

Use the real agent context at `agents/anne-lamott-neal-allen/memory/context.md`.
