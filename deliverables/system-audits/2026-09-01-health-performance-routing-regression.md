# Health Performance Routing Regression Repair

## Verdict

**Targeted repair:** `PASS`
**Whole-system verifier:** `PARTIAL` because of a pre-existing global wrapper drift outside this workspace lane.

The corrected Health Performance query now selects paid-social creative production in the command menu, workflow router, and routing governor. Alex Copper owns delivery. Static acquisition is bounded craft support. Claim-Safe Health Marketing remains a verification gate. Jun remains the owner when method mining is explicitly requested.

## Failure reproduced

Before the repair, the exact Method Card golden query ranked:

1. extraction-governor-agent;
2. source-to-skill-system; and
3. knowledge-librarian.

The phrase “recover the method” outweighed the more specific commercial output: five static ads plus one production-ready video concept for supplement and performance brands.

## Smallest repair

The machine binding now recognizes the exact delivery signature. A narrow paid-social creative lane gives that signature precedence over generic method-recovery language, and the command menu honors the same decision. The Method Card query now lives in the permanent golden-query matrix. Explicit `/jun-story-engine` and Claim-Safe compliance requests remain negative controls.

This does not make every supplement or health request route to Alex Copper. The route requires both the exact delivery shape and the creative-strategist/category context.

## Proof

The permanent suite passed all 86 golden queries. For the exact Health Performance query, both ranking surfaces put `alex-copper-performance-creative-production` first; the governor selected the `paid-social-creative` lane at 0.97 confidence. The two negative controls also held: an explicit Jun request chose `jun-story-engine`, while an explicit compliance request chose `claim-safe-pre-launch-compliance-gate`.

All changed routing modules compiled successfully, and the human-readable bindings appendix was regenerated from the machine source.

## Whole-system boundary

The full control-plane verifier passed the golden queries, bridge coverage, required text, Autopilot preflight, and local helper checks until `sync_global_operator_core.py --check`. That check failed because the global AGENTS wrapper is missing four end-session phrases: `Next-time prompt`, `Subagent worth it?`, `Reuse hook`, and `real Codex subagents require explicit authorization`.

Those files are outside the current workspace and were not changed. This unrelated global drift does not invalidate the targeted route repair, but it prevents a whole-system green claim.
