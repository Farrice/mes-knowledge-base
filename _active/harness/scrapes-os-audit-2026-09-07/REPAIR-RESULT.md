# Astra operating repair: what changed and what is still unproven

September 7, 2026 · **Verified in the repair lane; not deployed to main.**

The useful direction is a thinner operating layer around the existing expertise: recover the right history, reflect the intended result briefly, keep one owner, preserve corrections, and judge the actual output. A second complete OS has not earned replacement status.

## The repairs

| Failure reproduced | Change made | Evidence and limit |
|---|---|---|
| A fully provisioned work lane returned no episodic history because it used a different project identity. | Resolve the primary Git checkout, search its history plus the current lane, preserve deliberate overrides, and report unavailable or unindexed stores. Add episodic scope proof to lane parity. | Live: the repaired lane sees 25,061 indexed exchanges and the same five Scrapes record IDs as main. Mixed-project and malformed-setting controls prevent accidental cross-project search. Other memory stores were not replaced. |
| Native shell failures and patch/delegation events could be missed; full shell reads looked partial. | Normalize actual tool events; record explicit success/failure/running/unknown, local file production, successful delegation and shell polling completion. Distinguish complete reads from slices and truncated output. | Native/legacy fixtures plus a real adapter-to-ledger subprocess test pass. Duplicate delivery is counted once when an event ID or shell-session completion is available. Desktop dispatch remains untested. |
| Printed script names and receipt markers could look like completed work. | Require a recognized invocation of the workspace's actual script plus successful completion and its receipt. External same-named skills/scripts do not receive local credit. | Echo/source-reading and foreign-path negative controls pass. Complex shell command lists stay conservative rather than receiving guessed credit. |
| Your current request routed to Scrapes, and “intent” could make a launchpad replace an established system-audit owner. | Recognize specific runtime-repair language and preserve the chosen system-audit owner. Keep the actual requested outcomes in the working packet. | Your full message now has the same owner through routing and handoff, retains memory, execution records, private audit and output proof, and permits local execution. Content about Agentic OS remains content. |
| The question-answer envelope could route “All of the above” to an unrelated expert. | Exclude the completed clarification envelope from new routing. Preserve any separate new instruction outside it. | Full-envelope and mixed-message controls pass. This prevents a routing injection; the model still has to understand and retain the answer. |
| “Do not publish it or spend money” could block a local draft. | Share bounded action/negation parsing across both preflights and the launchpad. Recognize word boundaries and separate artifact nouns from later actions. | Local draft controls pass; “draft a message, then email it” and later publish/spend instructions remain permissioned. This parser never grants permission itself. |
| Instructions required an extensive visible packet while also demanding concise co-creation. A deferred verifier reappeared as mandatory. | Make the detailed packet internal, keep the opening mirror concise, retrieve approved context before asking again, preserve cumulative corrections, and reconcile the routing verifier list. Clarify that Claude model seating does not limit Astra. | Changed existing owners only: CODEX.md, the canonical launchpad contract and system-audit workflow. No new universal workflow, expert stack or global configuration was added. |

## Verification

[Focused regression receipt](REPAIR-TESTS.txt): **59 passed**. It includes negative controls and the actual current user request, not only documentation phrases.

The current control classifier, Google Operator Core, Codex authority and Autopilot runtime checks passed. Constitution lint reports no failures. These existing checks cover structure and selected routing behavior; their names and PASS statuses do not establish creative quality or every live hook's operation.

[Live memory and source receipt](REPAIR-LIVE-PROOF.json): matching main/lane record IDs, explicit project scope, scope parity and hashes for the five authoritative Jen source/decision files. The exact approved Jen caption remains unchanged.

The required legacy finalization entry is saved in REPAIR-FINALIZATION.txt. Its administrative 7/10 fields produced a marginal status; they are not human ratings or output-quality evidence.

The two read-only auditors reviewed separate areas. Their first reviews exposed malformed memory settings, false completion markers, external-path misattribution, native polling gaps, an email/DM negation regression and a content-routing regression. Those findings were repaired and added to the regression set. Root owns every edit.

## What “prove better work” currently means

**Proven:** the repaired code retrieves previously missing history and handles the named execution and routing cases correctly. The original approved Jen artifact and current corrections are recoverable without asking Farrice to restate them.

**Not proven:** higher creative quality, fewer revisions across live tasks, native desktop delivery of all recorded event types, or superiority over the private Scrapes OS. No numerical self-rating is being used as a substitute for those outcomes.

The existing task **Client: Jen B-Roll Post - Approved Voice Continuation** remains the client production owner. Its latest result found no new full-caption reference meeting the already-approved source and craft standard. This repair did not invent a new source, overwrite the approved post, or deliver a competing client draft to manufacture a win. The approved original succeeded before the later playbook existed; those later rules did not earn credit for that success.

A concrete source contradiction also remains visible: the generic Jen skill says every piece teaches something and applies broad register/engagement checks, while the current approved writing packet explicitly rejects a compulsory lesson, local reference or CTA for this post. The current task brief and Farrice's accepted work govern. The core repair now states this precedence; a wider client-skill rewrite is not bundled into this repair.

## Private Scrapes comparison

The purchased Skill Systems pipelines and Agentic OS are separate products. Installed skills, public OS documentation and the cached installer were audited; the private OS source was not available through the local filesystem or current GitHub access.

Signed-in Scrapes classroom access was confirmed. Its setup supplies an installer/token workflow rather than a source export. No token lesson was opened, credentials extracted, installation run or purchased material uploaded elsewhere. The installer's own supported saved-GitHub-credential lookup was also checked; it reported no credential available. The private code audit needs an accessible repository checkout/ZIP or authenticated access to `simonc602/agentic-os`.

The provisional judgment remains: keep the expertise and useful Scrapes production pipelines; simplify the current control layer. The vendor's documented scoped memory and simpler client context structure are promising design references, not evidence of a better Astra runtime. [Full comparison and sources](AUDIT.md).

## Deployment boundary

The reviewed work is in `codex/scrapes-os-audit-20260907`. Main has 122 unrelated tracked changes and 44 untracked paths at the last read. The existing preservation tool's dry run identified the tracked set; it did not move or restore anything.

The clean-main integration rule prevents quietly treating a tested branch as a deployed repair. The proposed integration is to preserve the unrelated tracked changes in a verified local branch, retain untracked work in place, integrate the reviewed repair locally, then observe actual desktop events. Moving that unrelated work needs a deliberate integration decision. No global settings or remote writes are included.

The adjacent **System: Astra Harness - Capability and Operating Audit** already disabled two global startup behaviors and independently verified them in a fresh task. Its local health/real-output-evaluation commit `5644e3fc158ca5c03e5c1585a4aca3d8a84138b9` is parked behind the same integration condition. This branch does not duplicate those edits.

## Next proof after integration

1. Observe a native completed shell failure, a real local patch, a complete versus partial source read, and their saved execution records in a fresh permitted context. A replay is not the live canary.
2. Run one meaningful task with the exact source packet and approved benchmark, followed by a bounded correction. Compare the full output, preserved decisions and avoidable questions. Keep taste unapproved until Farrice judges it.
3. Add a vendor condition only when the private source and Codex execution path are inspected. Match model, effort, source input, permissions and budget. Do not use the installed Skill Systems as a substitute for the missing OS condition.
