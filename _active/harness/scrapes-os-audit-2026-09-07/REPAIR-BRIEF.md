# Repair brief: make the existing harness dependable

Status: memory, execution records and co-creative routing repairs are verified in the isolated lane. See [current repair result](REPAIR-RESULT.md). Production readiness and broader outcome evaluation remain separately scoped; this original brief is not a claim that every listed item shipped.

## Intended result

A fresh Codex worktree retrieves the right project history, records native tool events accurately, rejects unusable production inputs, and preserves the active task across answers and corrections. The existing library, budgets, client boundaries, approved examples and repository protections remain available.

Root task owns integration. Any implementation must recheck other active repair lanes before edits. The earlier Astra audit is already handling the two global startup settings and may also address health/evaluation reporting; do not duplicate or revert that work.

## Priority 1: restore memory continuity

Owners: execution/memory_facade.py and the existing lane parity check in execution/worktree_lane.py.

- Resolve canonical repository identity independently of the checkout directory.
- Search the canonical project and current-lane identity where appropriate; deduplicate records.
- Preserve explicit ANTIGRAVITY_EPISODIC_PROJECTS overrides and project isolation.
- Detect an inaccessible or mis-scoped store as unknown/degraded rather than a successful empty result.
- Do not broaden default retrieval to every unrelated project.

Acceptance: in a temporary fixture containing canonical, lane-specific and unrelated-project rows, a new lane finds both permitted groups and excludes the unrelated group. The current known “Scrapes” query returns the same canonical record IDs from main and lane. A missing database reports unavailable. Lane bootstrap's proof cannot say full parity without exercising the stores it promises.

Preservation risk: a careless fix could recover history by reading all projects and silently break isolation. Test exclusion as well as retrieval.

## Priority 2: make Codex work observable

Owners: .codex/hooks.json, .codex/tools/codex_hook_runner.py where needed, execution/hooks/session_ledger_hook.py and existing tests.

- Normalize native event names and payloads at the platform boundary.
- Observe apply_patch and native delegation when the app supplies them.
- Honor explicit shell exit codes.
- Distinguish suggested, inspected, loaded, used and outcome-rated evidence.
- Do not treat arbitrary shell output or partial reads as proof of complete skill activation.

Acceptance: controlled fixtures for native patch, successful and failed shell, full/partial reads and delegation produce the intended records exactly once. A fresh-task live canary confirms actual desktop dispatch for supported events. Unobservable events remain unknown. Claude fixtures continue passing.

Preservation risk: simply adding more regex matches may produce convincing but false utilization data. The proof is an observed event and an appropriate state transition.

## Priority 3: make production readiness mean usable inputs

Owner: execution/scrapes_brand.py plus its existing tests.

- Validate minimum usable token/voice data using the vendor's actual schema.
- Check template files referred to by approved manifest entries.
- Check fallback renderer existence and expected entry point.
- Enforce brand ownership for output and pool locations.
- Explicitly allow legitimate shared research caches; do not silently permit unregistered foreign output roots.

Acceptance: the empty tokens, empty voice, missing renderer, ghost template and foreign-output fixtures fail for the correct reason. At least one real known-good pool passes and can produce a local artifact without paid generation. Preserve already approved brand assets.

Preservation risk: stricter generic validation can break supported vendor variations. Read the installed pipeline contract before choosing required fields.

## Priority 4: preserve intent across conversation turns

Owner: execution/skill_router_hook.py and its existing regression tests.

- Parse supported structured answer envelopes as answers to an existing question.
- Retain the active objective on clarifications and corrections.
- Recognize explicit objective replacement and direct skill invocations.
- Abstain when context is unavailable rather than manufacturing a specialist recommendation.

Acceptance: this audit's exact structured “All of the above” reply produces no positioning recommendation. A Jen failure example stays evidence inside a system audit. An explicit request to switch to Jen content does route there. Tiny acknowledgments remain quiet.

Preservation risk: overcorrecting by suppressing every follow-up could hide deliberate user changes. Test both preservation and replacement.

## Priority 5: align proof and instruction claims

Coordinate with the active Astra audit before touching these files.

Owners: existing system-audit workflow, system_health.py, verify_google_operator_core.py, weekly evaluation template and orchestration doctrine.

- Mark failed data reads UNKNOWN and unimplemented checks UNTESTED.
- Reserve behavioral PASS for observed task outputs.
- Reconcile canonical versus historical verifiers.
- Verify actual hook state/trust; distinguish a direct handler test from desktop activation.
- Scope model-specific roles to the platform they describe.
- Keep the already-disabled teaching default and automatic JARVIS startup state intact.

Acceptance: a forced Notion failure cannot become an empty performance database. Missing/disabled protection cannot inherit a complete protection PASS. Documentation-only cases cannot be counted as successful creative output. Canonical checks remain meaningful and proportionate.

Global instruction edits require explicit approval of a concrete diff. Do not edit vendor caches or introduce a second router.

## Small matched test before broader migration

Use the same Astra model/effort, inputs, tool access, action permissions and maximum work budget for each condition. Preserve accepted source material; do not ask the assistant to reconstruct a success from a summary.

Start with two conditions:
A. Current harness baseline, as captured by this audit.
B. Repaired/simplified existing harness.

Add C, isolated Scrapes OS, only after source access and its actual Codex execution path are verified. Do not substitute installed Skill Systems for the OS and call that condition C.

| Case | Work to complete | Inspectable quality bar |
|---|---|---|
| Creative transfer | One complete new Jen B-roll post from the approved specimen and current sources | Farrice judges the full artifact; source requirements and conversational craft both survive |
| Research and decision | One bounded current question using the same source packet/tools | Claims trace to sources; unsupported claims stay uncertain; a usable decision is delivered |
| Continuity and recovery | Correct a task mid-run, interrupt it, then resume in a fresh permitted context | Earlier decisions and approvals survive; no wrong brand or repeated permission request |
| Mechanical canaries | Memory, event accounting, readiness and routing negative controls | Every known failure is detected while valid controls pass |

Record completion, necessary/avoidable questions, correction loss, unsupported claims, rework, elapsed time and judged quality. Do not turn self-assigned scores into measured improvement. Label sample identities neutrally for creative judging where practical. Run a second matched specimen before generalizing a winner.

Do not launch quota-heavy repeated generations, a vendor installation, new hosted services, publishing or outreach from this brief. Prepare the test first and obtain the applicable execution authorization.

## Reversal and stop conditions

Stop expanding the repair when the named failures are fixed and the first matched tasks are reviewable. Revert a change that breaks isolation, approvals, known-good rendering or prior task behavior. Keep revisions reviewable by owner; avoid bundling memory, routing, global instructions and a new OS install into one experiment.

Recommend replacement only if the isolated vendor condition repeatedly preserves Farrice's context and accepted craft with fewer interventions and lower ongoing maintenance cost. No such result exists yet.
