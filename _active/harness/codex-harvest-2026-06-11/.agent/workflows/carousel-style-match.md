---
description: "Create reusable brand/style direction for AI carousel generation"
---

# /carousel-style-match

Use `/mood-board` or `/design-md-synthesize` first when the brand style needs to be durable. Then feed the style direction to `/carousel-design-prompt` or `/ai-carousel-engine`.

