# /virtuoso

Deploy the Codex Antigravity Virtuoso orchestration front door.

Canonical Codex workflow:

`/Users/farricecain/Codex Antigravity/.agent/workflows/virtuoso.md`

This legacy command file exists for compatibility and discoverability only.
Codex execution should read the canonical workflow, then run:

```bash
python3 execution/virtuoso_orchestration.py "[goal]"
```

Supported options:

- `--trace-only`
- `--delegate`
- `--log`
- `--mode revenue|creative|research|build|audit|repair`

Do not spawn real Codex subagents, publish, send outreach, use paid/API tools,
write connectors, mirror globally, mutate Mission, or edit
`/Users/farricecain/Google Antigravity` without explicit approval.
