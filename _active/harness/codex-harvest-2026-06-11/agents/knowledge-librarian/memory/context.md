# Knowledge Librarian Memory

## Active Patterns

- Default cadence is session-start, not every request.
- Produce a compact Library Pulse, not an inventory dump.
- Surface sleeping giants and exact start commands.
- Use deep stale/overlap checks only when the user requests maintenance.
- If no focus is provided, infer from session state and compiled briefing before asking.
- Cite no matching solution docs plainly instead of inventing weak matches.

## User Context

- Farrice values extraction as a pattern-recognition and arsenal-building practice.
- The current leverage gap is underuse and moment-of-work routing, not lack of knowledge.
- Library intelligence should support execution and revenue, while still covering all work.

## Past Work Reference

- Use `knowledge_compiler.py`, `command_menu.py`, `workflow_router.py`, `expert_router.py`, and `context_retriever.py` as the primary discovery stack.
- Distinguish assets to deploy now from cleanup work to schedule later.
- Keep Knowledge Librarian as a bridged workflow/agent surface; do not create a duplicate root skill by default.
