# Scrapes Skill Systems — Resume Brief (LIVING; update in place)

*Last updated 2026-09-03 (lane `worktree-scrapes-routing`; blind bar 01 built while Farrice slept, $0 in images). Paste the "Kickoff prompt" at the bottom into a fresh session to start hot.*

## Where we are, in one breath
The 36 Scrapes skills and 4 workers are vendored, on main, and — as of tonight — **reachable from intent**: six bindings open six front doors (`/social-carousel`, `/social-post`, `/social-repurpose`, `/deck-build`, `/video-to-shorts`, `/video-to-ebook`), every door opens with a **BRAND LOCK** (`execution/scrapes_brand.py`; farrice / jen / andrea registered; ambiguity asks, never guesses), our pens own the copy seams (Scrapes runs Scenario A), and every run writes to the loops (`context/learnings.md` is live, finalize, asset manifest, handoffs). Design + seam map: `ORCHESTRATION-DESIGN.md`. Jen's canvas thread is **parked** (his call, 2026-09-02); nothing further on her run until her six inputs land. **Blind bar 01 is built** (2026-09-03): three 7-slide AG1/greens teardown carousels on the editorial pool, A and C through the Scrapes designer + image generator, B through our direct render, all real evidence crops, no AI; judge at `_active/farrice-brand/content/blind-bar-01/JUDGING-SURFACE.html`.

## What he still has to tap (his verdicts, in order)
1. ~~Approve the editorial style~~ **DONE 2026-09-03: 11 templates approved in manifest.json, portrait-statement-cta retired (boxed portrait = low taste).** Studio follow-up (not built): true 4:5 / per-platform preview + a plain comment box, as OUR wrapper, never inside .claude/skills. Original note: **(8 templates)** — his aesthetic pivot 2026-09-03: the Canva "Creative Presentation" grammar re-solved for 4:5 (giant lowercase words, red script signature, real evidence crops, header/footer strip). Frames approved with notes → composition pass → 8 templates built pure HTML ($0) into the same `linkedin-carousel` pool as style `editorial`; `styles.json` splits editorial vs typographic. `REVIEW-NOTES.md` carries the decisions. Open Template Studio and Approve the eight (they sit at `status: ready`). Signature = Snell Roundhand until his handwriting lands in `visual-identity/logos/`.
1a. **Typographic pool (4 templates)** — BUILT 2026-09-03: 4 templates in `brand_context/templates/linkedin-carousel/` (cover · kicker stack · marker body · dark CTA), all pure HTML, $0 GPT Image. His first Studio pass: "typography, spacing, hierarchy done poorly on some" → craft pass done (headlines back to the brand's 72px h1, ref rhythm restored; `REVIEW-NOTES.md` in the pool). `scrapes_brand.py check farrice --pool linkedin-carousel` → READY (4/4). Open the Template Studio (`python3 .claude/skills/00-social-content/scripts/content-studio/content_studio.py brand_context/templates/linkedin-carousel --mode template --brand-context brand_context`) and click Approve per template. G2 warning stands: two image-bearing moves (evidence-crop, original-portrait) have no photo-zone template — add one photo ref later or flip them to `image_bearing:false`. Rule learned: headline values carry `<br>` line breaks or the renderer's autosize shrinks them.
1b. **`/scrapes`** is the one door (raw text in); user's guide `USER-GUIDE.md`.
2. **Blind bar 01 — VERDICT IN (2026-09-03).** Take A (AG1 named, fight picked) = the floor, "11 out of 10"; take B (AG1, symptom→decision) = close second, "I would close both"; take C (composite) = 9, "safe... gets buried", client-privacy use only. Captured: recipe card `docs/solutions/2026-09-03-teardown-carousel-copy-lock-evidence-crops.md`, door `/social-carousel` §Proven run shape (+ Step 2.5 verification hub, Step 4.5 composition pass), calibration-log rows, voice_ratchet ×3 (64 pending → run `/voice-compile`), memory `feedback_blind-bar-01-verdicts-fight-picking-is-the-floor`. **Next build (after his go):** composition/proportion + hierarchy pass on the editorial templates, pre-crop evidence to slot ratios so nothing important is cut off, `VERIFICATION.md` generator per run, then his word-level edits on take A's caption and ship A (and B) by hand. Content Studio on Take B: `http://127.0.0.1:59173/`.
2b. **Claude Design push — DONE 2026-09-03:** 50 files written to project "Farrice Cain — Premium Minimal" (077c3fae-5aa2-452e-8e92-073c33c6abc2) under `social/blind-bar-01/` (per take: index.html card, caption.md, png/, jpg/; plus README.md and claims-ledger.md). Same route for future runs. Original note: Bundle is rebuilt on demand: `uv run --with pillow python _active/farrice-brand/content/blind-bar-01/build_design_bundle.py` → `.tmp/design-sync/blind-bar-01/` (PNG + 1080x1350 JPG + captions + one `<!-- @dsCard -->` index.html per take + claims ledger). Build it AFTER the last lane merge or move it out before merging: 50 untracked files in lane `.tmp/` made `worktree_lane.py merge` time out in its check-ignore step (2026-09-03, twice). He runs `/design-login` once in an interactive `claude` TERMINAL on this Mac (the claude.ai site login does not count; verified 2026-09-03); then: DesignSync `list_projects` → pick or `create_project` "Farrice Cain · Social" → `finalize_plan` writes `blind-bar-01/**` with localDir `.tmp/design-sync` → `write_files` by localPath. Zip of the same bundle was sent to him 2026-09-03.
3. **Precedence map** — any FLIP / KEEP he disagrees with (`PRECEDENCE-MAP.md`).
4. **Jen v2 copy (cover A)** — 9 or not, which frame is short (thread `jen-canvas`, parked).
5. **Gigi** — register her `BRAND.yaml` when lane `worktree-gigi-engine-run` lands (her root is not on main yet).

## Do NOT rebuild
Brand context (all three), the six front doors, `scrapes_brand.py`, the bindings, the budget guard, the precedence map, the constitution block, the canvas artboards, the intake pack. Extend, never regenerate. Never edit inside `.claude/skills/*`.

## Harness state that matters
- `worktree_lane.py merge` now verifies its abort (main can no longer be left mid-merge by the tool); `worktree_lane.py preserve` moves stranded human work off main into its own lane; `main_write_guard.py --self-test` pins the cwd rules (21 cases). Tests: `tests/test_worktree_lane_merge.py`, `tests/test_scrapes_routing.py`.
- `.agent/openai-usage.json` is TRACKED and is written by `openai_budget_guard.py status/check` — it should join `SPEND_LINKS` in `worktree_lane.py` (untrack on main first) so lanes never churn it. Not done tonight; one-line follow-up.
- `tool-zernio-social/SKILL.md` frontmatter does not parse (unquoted colons); it is DO-NOT-USE anyway, so it is the one vendor skill the router does not index.

## Files
`_active/harness/scrapes-skill-systems/{ORCHESTRATION-DESIGN,PRECEDENCE-MAP,INTEGRATION,RESUME-BRIEF}.md` · `execution/scrapes_brand.py` · `.agent/workflows/{social-carousel,social-post,social-repurpose,deck-build,video-to-shorts,video-to-ebook}.md` · `context/learnings.md` · `_active/farrice-brand/content/blind-bar-01/` (takes A/B/C, ledger, judging surface) · `projects/00-social-content/2026-09-03/blind-bar-01-*/` (renders, plans, logs) · `brand_context/` + `_active/farrice-brand/brand_context/BRAND.yaml` (Farrice) · `_active/clients/jen-listings/brand_context/` (Jen) · `_active/clients/andrea-dj/brand_context/BRAND.yaml` (stub) · `directives/openai-usage-policy.md` · canvas https://claude.ai/code/artifact/084b1bd6-f6b0-4a4a-b1dd-9ddb6d611fb1 (parked)

## Kickoff prompt (paste into the new session)
```
Resume thread scrapes-integration. Read _active/harness/scrapes-skill-systems/RESUME-BRIEF.md. I have tapped my verdict on blind bar 01 (JUDGING-SURFACE.html): <paste verdict>. Apply it: voice_ratchet the hook verdict, fix the one change per take, and either ship the winner to Content Studio for my final pass or run round two. BRAND LOCK on every step.
```

<details><summary>Previous kickoff (2026-09-02, done)</summary>

```
Resume thread scrapes-integration. Read _active/harness/scrapes-skill-systems/RESUME-BRIEF.md, then ORCHESTRATION-DESIGN.md §3–4. First: build my linkedin-carousel template pool (refs from premium-minimal/package/templates/carousel, ssc-template-builder per ref, state cost before each GPT Image call, I approve in Template Studio). Then run /social-carousel on one supplement teardown for my brand as blind bar #1 against the current LinkedIn carousel path. BRAND LOCK on every step. No Chain ceremony until something ships.
```
</details>
