# Approved repair scope

User authorization: September 7 follow-up selected all three audit recommendations.

Outcome: remove two conflicting global injections; compare one existing/new Jen candidate with the approved specimen; make performance-data failure remain unknown and behavioral scores depend on observed execution.

Preserve: every unrelated global setting; JARVIS callable plugin; original Jen copy and sources; source task ownership; current publishing/spending/delegation protections; local report-only behavior.

Local ownership: main audit task edits execution/system_health.py, the existing weekly eval definition and card producer, one focused evidence checker and relevant offline regression tests. No domain routing, model seating, Mission runner, or client production edits in this repair.

Baseline failures: Notion exceptions produced count=0 and false downstream blocks; weekly E1/E2/E5 passes cited instructions rather than observed runs.

Acceptance: unavailable/malformed data stays UNKNOWN; a confirmed empty list still counts as zero; numeric readiness thresholds remain correct; unmeasured cross-pollination is not called blocked; unsupported behavioral passes are rejected and E5 is measured from actual response text. Global diff has exactly two intended changes. Fresh startup and Jen craft verdict remain separate observed checks.

Stop: one implementation pass plus a targeted repair pass if tests expose defects; no loop of speculative improvements. Global activation requires a fresh task's actual context; no claim from file presence alone. Jen taste remains Farrice's, independent of the audit's editorial assessment.
