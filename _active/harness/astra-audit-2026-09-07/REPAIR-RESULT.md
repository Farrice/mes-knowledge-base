# Astra repair results

September 7, 2026. The two global changes are live. Local code changes are tested; integration status is recorded below. Jen writing repeatability remains unproved.

## Changed and verified

- Disabled the global learning-output-style plugin and only the automatic JARVIS SessionStart hook. JARVIS itself remains enabled. The parsed configuration differs only in the two approved settings.
- Inspected actual startup developer messages in a new Astra/high Google project task: neither automatic injection is present. This verifies their absence in that fresh context; it does not prove every unrelated hook fires. Existing tasks retain already-loaded instructions.
- Changed performance-log errors and malformed data to UNKNOWN, while preserving real zero and numeric readiness thresholds. A live query failure now produces UNKNOWN downstream, not invented zero activity. Cross-pollination remains UNMEASURED because that data is not read.
- Corrected the existing weekly evaluation instructions and card producer. PASS/FAIL now require executed evidence. NOT_RUN and BLOCKED remain distinct. Added a local evidence validator that checks receipts, hashes, execution kind and E5 paragraph length; semantic judgment still requires inspection.
- Preserved the historical W36 transcript and explicitly marked its instruction-based E1/E2/E5 passes unsubstantiated in the current evaluation definition.

## Proof

Fourteen targeted offline tests pass, including unavailable, malformed and empty data; threshold boundaries; missing, altered and instruction-only evidence; and verbosity form failures. The Operator Core verifier passes after the two global changes. The safe E3 prose probe was FLAGGED and E4 parked a temporary T2 card without changing its body. The corrected score is **2 PASS / 4 NOT_RUN**, not six behavioral passes. Evidence and complete test output are in `evidence/`.

## Jen result

The latest complete candidate fails the approved craft comparison. The new production task verified all 289 transferred files and the exact approved specimen, but found no new qualifying complete source and produced no new post. Read `JEN-COMPARISON.md`. Its Google worktree bootstrap also hit a shared-registry permission limit, so full parity there is unverified. Removing conflicting instructions does not establish improved writing.

## Integration

PENDING: changes are in the isolated `codex/astra-harness-audit-20260907` lane. The normal lane merge will be attempted with no remote push. Shared main changes will not be overwritten.

## Remaining boundaries

Keyword follow-up routing, Astra seating language and other broader audit findings were not bundled into this repair. No paid calls, client-copy changes, publishing, external messages or collaboration subagents were used by this audit. Native task coordination was authorized. A separate requested startup verification task is still only acknowledged as queued; the actual fresh Jen task supplied the startup evidence instead.
