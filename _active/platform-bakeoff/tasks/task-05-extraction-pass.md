# Task 05 — Extraction pass (the system's signature workflow)

Source: first 500 lines of `extractions/transcripts/1p1llio3wnM.txt`.

Run an MES-style extraction pass:
- 5 genius patterns — each with: pattern name, the thinking behind it (their model, not a paraphrase of their words), 1 verbatim evidence quote from the transcript with approximate location, and a transferability note (where else it applies).
- 2 signature-workflow proposals — each: name, trigger ("use when..."), 4-6 concrete steps in the expert's logic.
- 1 "When NOT to use this expert" gate (3 bullet conditions).

Fabricating quotes is an automatic fail; quote only what is in the file. If the transcript's speaker is unidentifiable, extract the patterns anyway and mark the speaker UNKNOWN.
