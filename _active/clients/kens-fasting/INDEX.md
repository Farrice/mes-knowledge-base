---
status: parked
---

# Ken's Fasting Digital Product

## Purpose
Consolidated 2026-07-28 from three split locations (two `projects/` folders + the assembled `deliverables/` package). Strategy + sprint plan + swarm research at root; the client-ready package is `04-deliverables/client-package/`.

## Map
- `04-deliverables/`
- `swarm_research/`

## Filing
New artifacts go in the canonical subfolder at creation time (`directives/artifact-placement.md`), never loose at the project root. Moving anything: `python3 execution/project_filer.py plan --project "<abs dir>"` — never bare `mv`.
