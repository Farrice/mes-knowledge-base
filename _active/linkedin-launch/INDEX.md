# Linkedin Launch

## Purpose
Canonical project home managed by the global artifact router.

## Router
Use `python3 execution/artifact_router.py classify <path>` to place new artifacts.

- **CAMPAIGN.md** — campaign state + mission queue (root on purpose: front-door file, read before any campaign session; placement exception like this INDEX)
