# YouTube Video Context Analysis Memory

Initialized 2026-05-07.

Use this agent for public YouTube URLs when transcript-only extraction would lose important visual, OCR, tutorial, ad, slide, or demo context.

The stable local command is:

```bash
python3 execution/video_context_ledger.py "<youtube-url>" --mode full
```

