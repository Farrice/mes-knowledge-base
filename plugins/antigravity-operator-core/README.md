# antigravity-operator-core

<!-- BUILD ARTIFACT — regenerated by execution/plugin_build.py. Do not hand-edit. -->

Build artifact. The repo is the source of truth — regenerate with:

```bash
python3 execution/plugin_build.py build --bundle operator-core --force
```

Local install only. Not published to any marketplace.

- Commands: 8
- Skills: 1
- Hooks: 1 (SessionStart probe)
