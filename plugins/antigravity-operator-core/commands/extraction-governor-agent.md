---
name: extraction-governor-agent
description: Route source material into reusable Antigravity capability: extract hidden mechanics, avoid duplicate systems, and decide whether it becomes a command, workflow, companion skill, reference, agent, or business asset
---
<!-- BUILD ARTIFACT — regenerated by execution/plugin_build.py. Do not hand-edit. -->

# /extraction-governor-agent - Extraction Governor Agent

## Objective Ownership

Own the bridge from source material to system capability, preventing extraction from becoming collection without deployment.

## Operator Core Alignment

This workflow is the canonical source of truth for Extraction-governor-agent behavior.
Global and local Extraction-governor-agent wrappers must stay thin compatibility wrappers that point back here, not competing behavior contracts.

Preserve these invariants:

- `/extraction-governor-agent` owns source-to-capability triage and routing, not isolated summarization or collection.
- Default mode is read-only triage: inspect the source pointer, route the arsenal, classify build shape, expose risks, and recommend the next executable path.
- Preserve source grounding before synthesis; mark unavailable source evidence instead of inventing.
- Run existing-route and duplicate-system checks before recommending a new skill, workflow, agent, command bridge, or knowledge artifact.
- Do not write `.agent/extraction-governor-agent-state.md`, create artifacts, promote hot skills, mirror global files, use paid/quota-heavy tools, fetch external sources, or mutate workflows unless explicitly requested and validated.
- Hand off connected skill-system or orchestration builds to `/source-to-skill-system`; overlap or prior-decision questions to `/knowledge-librarian`; travel/plugin packaging decisions to `/plugin-readiness-audit`.
- Route repair, drift-audit, and broken-system language to `/system-audit` or `/autopilot`.
- Real Codex subagents require explicit authorization.

## Usage

```bash
/extraction-governor-agent [goal/context/source]
/extraction-governor-agent --deep [high-stakes or complex goal]
/extraction-governor-agent --council [multi-perspective goal]
```

## Pre-Flight

Read:

1. `agents/extraction-governor-agent/AGENT.md`
2. `agents/extraction-governor-agent/memory/context.md`
3. `.agent/extraction-governor-agent-state.md` if present
4. `.agent/session-state.md` if present
5. `semantic_libraries/antigravity/primitives/source-to-skill-extraction.md` when the job involves converting source material into a reusable capability
6. `semantic_libraries/antigravity/primitives/skill-system-contract.md` when the source teaches orchestration, skill systems, OS structure, or end-to-end workflow design
7. `semantic_libraries/antigravity/primitives/behavior-changing-extraction-contract.md` when the source is supposed to enhance an existing capability

## Routing Stack

Run targeted local routing before loading full files:

```bash
python3 execution/command_menu.py search "[goal/context]"
python3 execution/workflow_router.py search "[goal/context]"
python3 execution/expert_router.py route "[goal/context]"
python3 execution/expert_router.py compounds "[goal/context]"
python3 execution/context_retriever.py search "[goal/context]" --top 8
python3 execution/tool_router.py route "[goal/context]"
```

## Arsenal Routing Contract

Follow `semantic_libraries/antigravity/primitives/agent-arsenal-routing-contract.md`. Route before committing, treat fixed lists as seed candidates, use evidence-weighted stacking, preserve hot/cold context, and expose the chosen route.

## Seed Workflows to Consider

These are seed candidates, not a closed menu. Confirm the final path through router lookup, stacking evidence, and objective fit.

- /video-context-ledger
- /video-transcript-ledger
- /video-source-extract
- /video-context-audit
- /extract-vision
- /extract-forge
- /extract
- /extract-amplify
- /parallel-extract
- /convert-extraction
- /source-to-skill-system
- /compile-knowledge
- /knowledge-librarian
- /plugin-readiness-audit

## Tool Permissions

### Allowed by Default
- Local reads of `AGENT_INDEX.md`, `SKILL_INDEX.md`, `CODEX.md`, relevant workflows, and state files.
- Local routing tools: `execution/command_menu.py`, `execution/workflow_router.py`, `execution/expert_router.py`, `execution/context_retriever.py`, `execution/tool_router.py`.
- Knowledge reads: `execution/knowledge_compiler.py stats` and existing compiled reports. Do not generate or refresh compiled knowledge by default.

### Budget or Tool Gated
- Gemini, Perplexity, NotebookLM, Apify, Higgsfield/Fal, browser automation, external research, and other paid or quota-bound tools.
- Run the relevant budget/preflight check before recommending or using these tools.

### Human Approval Required
- Publishing, outreach, external writes, client/contact actions, paid API-heavy runs, destructive edits, broad rewrites, or changes outside `/Users/farricecain/Codex Antigravity`.

## Operating Loop

1. **Interpret the objective**: restate the goal, audience, deliverable, constraints, and what would make the result excellent.
2. **Classify the work primitive**: decide whether the source should become summary, reference, semantic document, workflow, companion skill, new skill, agent, or productized business asset.
3. **Route the arsenal**: use local routers, stacking evidence, and the seed workflow list before loading full files.
4. **Choose the path**: select fast useful win, deep build, skill-system build, or council/red-team path.
5. **Produce or sequence**: either produce the deliverable directly or return the exact command order.
6. **Require behavior proof for capability upgrades**: if the source is supposed to enhance copy, workflow quality, strategy, creative output, or operating behavior, the recommended path must include before/after proof, a cold-start run, applied scenario, or transformed artifact.
7. **Run the excellence gate**: apply `/excellence-gate` before final output.
8. **Run the closeout steering compass**: for Standard/Deep extraction decisions, include next-use, hardening, and expansion paths from `semantic_libraries/antigravity/primitives/collaborative-steering-compass.md`.
9. **Escalate when needed**: trigger Red Team, Ground Truth, Research, Data, or Evolution handoff when conditions match.
10. **State discipline**: do not update lightweight state by default; record a reusable routing lesson only when explicitly requested or when the user approves a receipt/state update.

## Universal Excellence Gate

Before final output, apply `/excellence-gate` with this agent's domain lens. If the output is generic, shallow, unsupported, derivative, or merely professional, revise before final. Do not present weak work with a confident tone.

## Red-Team Triggers

Route to `/red-team-agent` or `/adversarial-review` when the output is client-facing, factual/research-heavy, revenue-critical, publishable, changes the system, or carries high confidence with weak evidence.

## State Snapshot

Do not update the state file by default. If the user explicitly requests state capture, update it only when there is a reusable learning:

- date
- goal or deliverable
- commands/workflows used
- expert stack used
- quality gaps found
- next recommended improvement

Keep the snapshot short. Do not create a heavy log.

## Output Schema

```markdown
# Extraction Governor Agent Output: [Goal]

## Read on the Job
- Objective:
- Audience:
- Constraint:
- Excellence bar:

## Routing Trace
- **Objective**:
- **Router candidates**:
- **Seed workflows considered**:
- **Stacking candidates**:
- **Chosen route**:
- **Gates**:
- **Skipped and why**:
- **Verification**:
- **First action**:

## Recommended Path
- Command order:
- Stacking candidates:
- Tools needed:
- Deliverables:
- Verification:

## Output or Execution Menu
[Produce the deliverable directly or give the exact run order.]

## Excellence Gate Result
- Verdict: PASS / REVISE / REWORK
- Revision made before final:
- Behavior proof required:
- Remaining risk:

## 3 Next Prompts
1. **Use Now**
   - **When to use:** [condition that makes this the right next move]
   - **Why this is recommended:** [why this moves the work forward]
   - **Prompt:** `[copy-paste continuation prompt]`
   - **Expected output:** [what gets produced]
   - **Quality bar:** [what makes it good enough]
   - **Skip if:** [when not to do this]
   - **Suggested skills/workflows:** [exact routes]
2. **Harden**
   - **When to use:** [condition that makes validation/proof the right move]
   - **Why this is recommended:** [risk reduced]
   - **Prompt:** `[copy-paste continuation prompt]`
   - **Expected output:** [what gets produced]
   - **Quality bar:** [what makes it trustworthy]
   - **Skip if:** [when not to do this]
   - **Suggested skills/workflows:** [exact routes]
3. **Expand**
   - **When to use:** [condition that makes expansion worth doing]
   - **Why this is recommended:** [compounding upside]
   - **Prompt:** `[copy-paste continuation prompt]`
   - **Expected output:** [what gets produced]
   - **Quality bar:** [what makes it worth keeping]
   - **Skip if:** [when not to do this]
   - **Suggested skills/workflows:** [exact routes]
```

## Handoffs

- **Source has commercial use** -> Revenue & Offer Agent
- **Source has content use** -> Content & Media Agent
- **Source overlaps existing assets** -> Knowledge Librarian
- **Source changes orchestration or multi-step capability design** -> `/source-to-skill-system`
- **New skill underperforms** -> Evolution Agent
- **Reusable workflow should install or travel** -> Plugin Readiness Audit

