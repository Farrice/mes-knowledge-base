# Full-video Gemini trial — Paddy Galloway

User authorization: full video Z2uoA3bhJT0, extraction and connected skill build; total external spend capped at $10, 2026-09-14. Scope starts at 0 and ends at 9908 seconds. No timestamp cutoff.

One owner: Codex; no additional agents, background schedules, new dependencies or global installs. Source artifacts remain in extractions/paddy-galloway/youtube-masterclass. Raw captions preserve exact provenance; model video observations are separate and independently sampled for validation. Derived workflows cite timestamp evidence. Promotional claims remain attributed, not independently verified.

Components: existing watch/caption capture; content-analysis pilot shared utility layer; bounded Gemini static transport; source-to-skill-system; Paddy focused workflows; existing Briar/Kallaway/content packaging routes only at explicit handoffs. Order: source → perception proof → extraction → focused workflows → registration → applied demonstration → verification. User consumes a readable report and first-use route.

Engineering packet: extend the pilot with an explicit qualified static trial module. Separate real ledger from simulator but reuse canonical hashing, integer money and shared-state location. Each paid attempt reserves $1.25 irrevocably before HTTP dispatch; max six attempts = $7.50, leaving $2.50 unused buffer. Model fixed gemini-3.8-flash; one candidate; no external tools, retries, background processing or automatic fallback; max output 16384; static input context bound 1,048,576 tokens and conservatively reserve full 65536 model output tokens including thought. At verified $0.75/M input and $3.75/M output this is $1.032192/request, below $1.25. Pricing expires 2027-01-01. Never release reservations on unknown outcomes. Error or absent usage stops further dispatch until reconciled; no claim that local timeout stops remote billing. Ledger is shared across lanes and single-flight locked. Fixed source and job prevent this grant becoming a general allowance.

Agentic navigation is NOT authorized for dispatch absent a verified execution bound: current Interactions OpenAPI exposes max_output_tokens but no total processing/tool-input budget for video; the max_total_tokens field belongs to the separate Antigravity agent. Do not mislabel static video understanding as agentic navigation. This constraint may leave agentic parity unproven even when full-video extraction succeeds.

Validation: no-approval, source mismatch, cap, interrupted reservation and duplicate guards; actual API request/response/usage preserved; whole-timeline chapter coverage; caption/visual claim spot checks; source-backed extraction and applied before/after artifact. Highest proof is RUNNABLE with demonstrated local scenario, never proven virality or guaranteed expert equivalence. Reuse on-demand only. Existing watch remains intact; rollback removes additive module and links while retaining receipts.

## Observed result

One full-video request dispatched. Local read timed out after 600 seconds; no
response, interaction ID, visual observations or usage returned. Actual charge
is UNKNOWN. The $1.25 reservation remains; no automatic retry or fallback ran.
A full transcript was acquired separately and supports the extraction. This
proves neither Gemini quality nor completion of the requested perception test.

## Recovered result: current status

The single request completed remotely and its actual video response was recovered
from Google AI Studio without another paid call. Independent checks found wrong
timestamps and unsupported rules. Video-input processing is verified; precise
extraction quality failed this sample. Static processing was tested; agentic was
not. The usage-derived estimate is about $0.81, but billing remains unreconciled
because Google's counters conflict. The $1.25 reservation and dispatch hold stay.
Read `extractions/paddy-galloway/youtube-masterclass/gemini-quality-review.md` and
`gemini-recovery-receipt.json` for the current evidence. Earlier timeout statements
above describe the transport event, not the final provider status.
