---
name: Own the outcome through the next useful proof
problem_signature: capable executor prematurely recommends services or hands work back before checking execution access and the smallest private build
domain: system
date: 2026-09-08
status: verified-in-lane
---

# Outcome ownership hook

Farrice approved installation after the Rosita listing-film discussion exposed
late access checks, insufficient comparison of an assistant-operated private
tool with outsourced app development, and conclusions following mismatched
photo/prompt tests. The MES diagnosis also demonstrated why initiative must
preserve explicit read-only scope.

The canonical instruction lives in the marked `outcome-next-proof` section of
`semantic_libraries/antigravity/primitives/operating-alignment-contract.md`.
The existing Codex hook runner augments the skill-router response with that
section, and runs a bounded observer after session-ledger Stop. Existing
registrations, decisions, exit statuses, and other targets are preserved.

No global configuration, dependency, hook registration, real subagent, paid
service, or automatic corrective mutation was added. Set
`OUTCOME_NEXT_PROOF_OFF=1` to disable only this companion.

## Verification

- `python3 execution/verify_outcome_next_proof.py`: 22 tests passed. Includes
  real-adapter subprocess integration, native Codex and Claude transcript
  handling, listing failure cases, read-only controls, trivial-turn silence,
  wrong-session rejection, prior-turn isolation, missing evidence, failed tools,
  unrelated successful activity, kill switch, deduplication, and fail-open output
  preservation.
- Actual adapter plus actual skill-router controlled invocation: exit 0,
  `UserPromptSubmit`, approved contract and read-only constraint both delivered,
  1,446 context characters, no stderr.
- `python3 execution/verify_codex_authority.py`: PASS.
- `python3 execution/verify_google_operator_core.py`: PASS.
- Hook configuration unchanged. Controlled invocation is proof of the delivery
  path, not proof a new ordinary Codex turn automatically received it.

## Limits and acceptance

The observer reports `REVIEW_REQUIRED`, `NO_FLAG`, `UNOBSERVED`, or `SKIP`.
It never reports quality PASS. It recognizes bounded text cues and matched
successful retrieval calls, not the semantic sufficiency of research. Wrapped
tool calls it cannot interpret remain unobserved; false positives and false
negatives are possible. Human-quality improvement is UNTESTED until inspected
in ordinary work. The instruction is preventive; the observer is advisory.

It reads only a bounded transcript tail, resets evidence on the current user
turn, ignores reasoning/commentary as action proof, and writes hashes/flag names
to the existing observation log rather than copying conversation content.

## Installation boundary

Developed in `codex/outcome-next-proof` using the prescribed lane bootstrap.
The shared main checkout contained unrelated tracked changes before this work.
Only a successful guarded integration and a main-path probe establish installed
status. Never describe a parked lane as live. See the lane's
`.scratch/outcome-next-proof/` receipts for the actual integration result.

## Reuse

For future initiative complaints, inspect an actual task's unanswered objective,
tool-access evidence, validity of the test, and where work was handed back.
Do not add tool-count requirements or another broad checklist. Re-run the
focused verifier before changing the companion or adapting another harness.
