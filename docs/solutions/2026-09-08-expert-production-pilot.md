# Expert production: recover capability and preserve complete contributions

## Problem

Mailroom's native-team bridge survived on a branch, while main retained the older
council path. Its default briefs took capped prefixes of genius/memory files.
The older synthesis path omitted full revised contributions. Neither structural
health nor peer messaging established better client output.

## Change

Selectively recovered five Mailroom files, preserving existing council defaults.
Added an opt-in `persona_team.py production` compiler backed by the stdlib
`expert_production.py`: selected full methods, explicit client and relevance,
required execution/source/approval context, excluded-example separation, hashes,
and native-platform instructions. No dispatch, external actions or automatic
memory writes occur in the compiler. Existing expert-composition guidance points
to the production companion when explicitly authorized.

Full source, maker and editor contributions flow to the maker for final revision.
Coverage checks require an explicit disposition for each contribution. Original
message comparisons are optional and separately labeled; absent raw message
receipts never become byte-for-byte fidelity proof.

## Live evidence and correction

The first Jen pilot used one native solo executor and three production roles
(source worker reused from the shared preflight). All received the same frozen
evidence and selected methods with inherited model settings. Source-to-maker,
maker-to-editor and editor-to-maker delivery were observed. The maker's return
acknowledgment to the completed source worker hit the native agent-thread limit;
the conductor retained its full text. This is a successful partial messaging
path with a documented fallback, not universal runtime parity.

The maker independently caught that the conductor had placed a source summary
in the stored handoff. The author returned the full original; it was restored
before delivery. The maker had the complete original direct message throughout.
A negative control now rejects a substituted summary when original messages are
supplied to the checker. This incident is not concealed by the coverage PASS.

The editor and maker removed an unsupported repair-free-condo implication and
candidate-noise ambiguity while preserving concrete voice choices. Both final
variants passed the deterministic prose check. This is evidence of specific
changes, not evidence that the team version is better.

## Verification and present limit

- 28 focused tests pass, including CLI compatibility and negative controls.
- Google Operator Core and Codex authority checks pass.
- 5,876 protected originals/V2/source and approved asset files match baseline.
- Claude live execution is NOT_RUN; compatibility mapping only is tested.
- Native token use and active human correction/preparation time are UNMEASURED.
- First A/B pair awaits Farrice's judgment; second B-roll and carousel remain
  gated by acceptance. Adoption is UNPROVEN.

The complete current evidence, anonymous comparison and separate posting states
are in Jen's existing studio under `2026-09-08-swarm-pilot/`. No extraction,
original/V2 prompt or approved client asset was edited. No new subscription,
paid call, publication, global configuration or skill rewrite was introduced.

## Reuse and rollback

Use the production mode only for explicitly approved assignments. A source-file
read receipt does not establish appropriate use, and a coverage check does not
establish craft. Record human preference and observed active work for every
paired piece. One bounded repair is allowed; ties and missing measurements do
not qualify for adoption. Keep any successful rollout Jen-local initially.

Revert the owned recovery/production change set to remove this companion; do not
restore whole historical branches or alter unrelated client work. The lane is
retained until the human pilot completes, keeping its frozen packet inspectable.
