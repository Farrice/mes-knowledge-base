# Mood Board Orchestrator Repair Contract

## Integration Addendum — 2026-09-01

The original repair correctly restored actual board construction, blind taste
selection, and proving-surface behavior, but its broad discovery-routing premise
was superseded by newer main authority. Discovery-backed brand direction now
routes to `/andrew-lane-design-systems`, which may compose `/mood-board` as a
bounded visual-board builder. Standalone campaign, shoot, event, product, and
non-brand moodboards remain owned by `/mood-board`. Both dedicated verifiers
must pass. The original packet below is retained as historical implementation
evidence, not current routing authority.

## Preservation Lock

- **Keep:** `/mood-board` as the existing public front door; `creative-direction` as the function owner; Oren's reference-acquisition and board-conversation methods; Refero's research-first reference lock when available; the five-layer visual grammar; blind `Choose / Keep / Kill`; one proving surface; explicit human taste approval.
- **Change:** replace the text-only five-layer sequence with a connected, reference-first orchestrator that produces actual visual boards when visual tools or source images are available.
- **Do not disturb:** `/moodboard-sweep` remains the post-generation style-library test; `/design-md-synthesize` remains downstream tokenization; `/build-bos` remains the full Brand Operating System owner; Andrew Lane remains an optional post-selection decision-documentation layer and cannot own moodboard creation.
- **Risk:** an overbroad binding could steal full-brand, UI, style-library, or campaign-production requests; an overgrown workflow could become a mega-skill and dilute scarce taste ownership.
- **Gate:** positive moodboard-from-brief/discovery queries must rank `/mood-board` first; negative controls must retain their existing owners; a text-only strategy memo cannot be called a completed moodboard when visual reference access exists.

## Goal Packet

| Field | Decision |
|---|---|
| `target` | `.agent/workflows/mood-board.md`, its thin command bridges, `skills/creative-direction` moodboard prompt/contract, the narrow `routing_enforcer.py` binding, and a dedicated verifier. |
| `scope` | Connect existing reference research, visual territory construction, taste review, proving surface, and downstream handoff. Exclude new commands, new expert skills, global `~/.codex` mirrors, paid generation, public deployment, and broad router refactors. |
| `per_item_criteria` | Every changed surface must preserve one owner, name bounded components, pass exact positive and negative routing fixtures, and expose a cold-start usable sequence without hidden chat context. |
| `permitted_side_effect` | Workspace-local edits in `codex/mood-board-orchestrator-repair`; generated routing appendix refresh; local verifier and receipt output. |
| `proof_artifact` | `execution/verify_mood_board_orchestrator.py` output plus this contract and the final run receipt. |
| `measurable_stop` | Dedicated verifier passes all structure, routing, preservation, and negative-control checks; relevant existing control-plane checks pass; `git diff --check` passes. |
| `turn_cap` | Three patch-and-verify loops. If the third fails, park the lane with exact failures. |
| `evaluator` | Deterministic stdlib verifier, live `command_menu.py` and `workflow_router.py` queries, routing-enforcer checks, and existing skill/control-plane verifiers. |
| `wake_up_check` | `python3 execution/verify_mood_board_orchestrator.py` |
| `human_checkpoint` | Farrice authorized the workspace-local repair. Every future moodboard run still stops at blind taste selection before downstream build or public use. |
| `rollback_or_archive_rule` | Never delete the prior systems. If proof fails, leave the branch parked or discard the isolated lane; do not merge partial behavior. |

## Agentic Engineering Packet

| Field | Decision |
|---|---|
| Objective | Make `/mood-board` reliably turn a supplied objective, discovery brief, or reference set into three genuinely different, reference-locked visual territories, a blind taste decision, one proving surface, and a reusable handoff. |
| Source truth | Existing workflow and prompt; `skills/oren-taste-development/workflows/moodboard-conversation-system.md`; `skills/oren-slop-era-creative-strategy/workflows/reference-edge.md`; installed Refero visual workflow; recovered blind A/B/C precedent; failed Andrew Lane pilot; current router outputs. |
| Context plan | Keep the orchestrator, contract, current brief, and compact handoffs hot. Load one component only at its phase. Keep full expert dossiers, raw source packages, unrelated brand systems, and generation backends cold until needed. |
| Work chunks | (1) contract and routing fixtures, (2) workflow/prompt/bridge patch, (3) deterministic verifier, (4) live route and control-plane proof, (5) structure cleanup. |
| Review loop | Maximum three iterations. Pass requires all dedicated fixtures plus relevant existing checks; failure returns to the smallest affected file. |
| Dependency gate | No new dependency. Refero and image generation are capability-detected and optional; no install, paid call, or quota use is authorized by this repair. |
| Structure pass | Confirm no duplicate command, no duplicate expert skill, no Andrew Lane front-door authority, compact handoffs, and future-agent readability. |
| Use-now artifact | Repaired `.agent/workflows/mood-board.md` invoked through the existing `/mood-board` command. |
| Hardening proof | Dedicated verifier, router searches, routing-enforcer positive/negative checks, prompt/skill validation, system-control-plane check, and run receipt. |

## Skill System Contract

| Field | System decision |
|---|---|
| Source evidence | Existing local moodboard workflows and prompts; Oren personal-brand blind-direction artifacts at commit `607215a43`; Refero visual workflow version 1.0.2; failed brand-direction pilot at `7942ce2f0`/`28132e057`; user verdict that the new output was amateur and below the prior taste bar. Human preference for the exact prior loved session remains pending, so the recovered A/B/C board is a preservation candidate rather than invented certainty. |
| Objective | Provide one reusable moodboard front door for any brand, campaign, product, content, space, or creative objective without replacing specialized downstream owners. |
| Components | `creative-direction` conductor and visual grammar; Oren reference edge; Refero or available reference research; Oren moodboard conversation/taste approval; visual artifact creation through available local media/artifact tools; optional proving-surface owner; `design-md-synthesize` downstream; `moodboard-sweep` only after reusable style assets exist; Andrew Lane optional after selection. |
| Step order | intent lock -> reference acquisition -> three territory hypotheses -> reference locks -> actual visual boards -> blind choice -> proving surface -> selected-direction handoff -> optional style-library characterization. |
| Inputs | Objective/brief, audience, desired feeling, constraints, existing brand truth, supplied references/assets, decision owner, and target proving surface. |
| Outputs | Research ledger, three materially different territory boards, five-layer execution spec per territory, blind choice card, proving-surface test, selected-direction lock, and downstream handoff. |
| Handoff summary | Each phase passes exact source paths/URLs, compact decisions, rejects, proof state, next input, and open risk—not full upstream context. |
| Composition rule | `creative-direction` is sole function owner. Oren owns scarce taste/sign-off; reference providers own evidence acquisition; downstream builders own implementation. Supporting components may change only their assigned decision. |
| Human checkpoint | Blind `Choose / Keep / Kill / Reason` after three boards; explicit approval before paid generation, public use, publishing, purchases, or irreversible implementation. |
| Validation | Dedicated verifier plus router, routing-enforcer, skill contract, authority, and system-control-plane checks. |
| Behavior-changing proof | Cold-start query must route to `/mood-board`; the workflow must refuse the failed text-only path and output the connected phase/handoff contract; negative queries must continue routing to `/build-bos`, `/moodboard-sweep`, and `/design-md-synthesize`. Human taste preference remains `UNTESTED` until a real board run is chosen. |
| Result surface | A visual review board plus concise decision document in the current conversation/artifact surface; local source copies when persistence is needed. |
| Context policy | Orchestrator and contract hot; current references and one phase component on demand; raw extractions, full expert corpora, unrelated workflows, and generation backends cold. |
| Reuse hook | Existing `/mood-board`; no new command. Downstream routes consume its selected-direction handoff. |

## Evolution Council Verdict

- **Forager Digest:** the arsenal contains all major components, but no front door owns their order; router proof shows generic briefs outranking `/mood-board`.
- **Build Hypothesis:** repair one workflow, its thin bridges, its execution prompt, and one narrow binding; add one dedicated verifier.
- **Risk Review:** the plausible failure is replacing fragmentation with a mega-workflow or hijacking adjacent design/brand queries. Narrow positive signals and explicit negative fixtures are required.
- **Change Scope:** workspace-local files only; no global mirror, new command, new dependency, paid generation, or deletion.
- **Evaluation Plan:** deterministic positive/negative fixtures plus existing control-plane checks; maximum three loops.
- **Target:** existing `/mood-board` route and its supporting contract.
- **Goal packet complete:** yes.
- **Recommended path:** bounded workspace-local system repair.
- **Permitted side effect:** edit and verify only the named surfaces.
- **Proof artifact:** `execution/verify_mood_board_orchestrator.py` and final run receipt.
- **Stop condition:** all named checks pass with no routing regression.
- **No-regression check:** full BOS, DESIGN.md, and style-library queries retain their owners.
- **Human checkpoint:** already satisfied for local repair; future visual selection remains human.
- **Open risk:** deterministic proof cannot establish human taste preference; the first real run must remain explicitly `UNTESTED` until Farrice chooses a direction.

## Systems Thinking Trace

- **Wider outcome:** a reusable creative foundation that makes the next 20-50 assets easier, not one attractive board.
- **Assumption changed or confirmed:** the missing capability is orchestration and ownership, not another moodboard expert.
- **Craft owner and scarce judgment:** `creative-direction` conducts; Oren owns taste and the human decision conversation.
- **Paved default:** research-first, three distinct territories, blind selection, proving surface, selected-direction handoff.
- **Human outcome owner and recovery boundary:** Farrice owns the taste verdict; the branch can be parked without touching main.
- **Learning decision:** fix the upstream route and result contract; do not add a new permanent command after one failed artifact.
- **Remaining risk:** the exact loved prior session is not confirmed, so human preference is not claimed as proven.
