# execution/archive — deliberately delisted, not deleted

Assets moved here failed the two-sided grounding test (no `# consumer:` — nothing
acts on their output; no `# scar:` — no dated wound they guard). Council-ratified
2026-07-28 (knowledge/council-sessions/2026-07-28-seating-charter-*.md).
Restore = `git mv` back. /arsenal restocks anything that turns out load-bearing;
a real break is the ticket back.

- verify_steering_compass.py, verify_contextual_next_prompts.py,
  verify_global_steering_closeout.py (2026-07-28): asserted /steering-compass
  ranked top-3 in routing; red for days; keep-test found zero findings that ever
  changed a shipped artifact.

- 47 execution-class wiring orphans (4 restocked same-day: client_package_lint, review_miner, design_md_extract, cos_reminders — live memory/solution cards cite them) (2026-07-28, council "archive wholesale" +
  Farrice's one-sweep approval): zero firing paths (wiring_audit), zero import/
  invocation consumers (repo-wide grep incl. launchd + audit layer). Notables:
  polymarket_* (betting stack superseded), claude_export_* (import complete,
  never re-import), wave5_*, enforce_trial (compass doctrine: trials disarmed),
  paths.py (imported by nothing). Rescued from the sweep: wiring_audit.py
  (consumer: self_heal + pending_decisions_hook). 8 others kept with live
  consumers: ai_misfire_sprint_guard, audit_extraction_integrity,
  generate_agent_stacking_registry, perception_engineering_bridge_audit,
  publishable_copy_guard, system_efficiency_benchmark, test_video_enhance,
  weather_data.
