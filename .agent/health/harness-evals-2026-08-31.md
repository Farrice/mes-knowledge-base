# Harness Behavioral Evals — 2026-08-31 (W35)

| Eval | Verdict | Evidence |
|------|---------|----------|
| E1 — Router fires on core domain | **PASS** | `match_bindings("write a LinkedIn post...")` returns `linkedin_from_scratch` → ghostwrite/lara-acosta; binding signal "write a linkedin post" matches |
| E2 — Intent mirror on raw dump | **PASS** | `_mirror_signals` returns 2 on 150-word ramble; `_mirror_block` injects INTENT BRIEF (do NOT produce yet) |
| E3 — Slop ban catches banned phrasing | **PASS** | `prose_classifier.py check` on 81-word draft with 5 banned patterns → FLAGGED, AI Score 9.5/10 |
| E4 — Refusal net parks outward action | **PASS** | `FORBIDDEN_RE` fires on "publish"; note: spec phrase "post this to LinkedIn" gaps the "post to" pattern — consider widening regex to `post.*to` |
| E5 — Verbosity register holds | **PASS** | Demonstrated 67-word answer to "what does execution/notify.py do? one paragraph" — no headers, no bullets |
| E6 — Memory recall surfaces standing rule | **PASS** | Sovereign memory returns "twice-buried corpse wearing a new name" (score -16.65) on "critic agent fleet" query |

**Result: 6/6 PASS**

## Notes

- E4 gap: `\b(post to)\b` doesn't match "post this to LinkedIn" — the intervening word breaks the match. Not a failure (mechanism works on semantically equivalent "publish"), but worth considering a looser pattern.
- No prior scorecard exists for trend comparison (first run of eval_set_v1).

---
_Generated by harness eval run, 2026-08-31_
