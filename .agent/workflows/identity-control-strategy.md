# /identity-control-strategy

Run the **Identity Control Strategy** workflow — the top-down (Stussy) vs. bottom-up (Lo Life) tribe-control fork, plus the mandatory mirror-test ethics gate.

## Instructions
1. Load `skills/oren-identity-brand-os/genius.md` — Top-Down vs. Bottom-Up Tribe Control, Third-Space Replacement, the Mirror Test, Homie Lookbook
2. Execute `skills/oren-identity-brand-os/workflows/identity-control-strategy.md`

## Produces
Fork diagnosis + decision-criteria table + hybrid play (if warranted) + mandatory Mirror-Test PASS/FAIL verdict

## Stacking
- Upstream: requires a driver from `identity-driver-diagnostic` and tribe evidence from `true-fan-density-engine`
- Downstream: `identity-scale-router` once a control strategy is validated
