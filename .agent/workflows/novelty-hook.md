# /novelty-hook

Compile a bank of 1–2 line hooks that pack New Reveal + Outcome + Contrast (+ real urgency) at maximum density.

## Trigger
`/novelty-hook`

## Workflow
`skills/kallaway-illusion-of-novelty/workflows/novelty-hook.md`

## Quick Use
Provide:
1. Topic + chosen angle
2. The held belief + the wanted outcome
3. Whether a real urgency window exists (y/n)

## Output
10+ annotated hook variants (which components each carries), density-sorted, with the single recommended pick + rationale.

## Stacks With
→ `/novelty-reveal` + `/novelty-contrast` (supply the inputs)
→ hook-engineering-matrix (kallaway-content-psychology — neurochemical curiosity-gap variant)
