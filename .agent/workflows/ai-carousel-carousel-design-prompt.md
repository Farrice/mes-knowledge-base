---
description: "/ai-carousel-carousel-design-prompt — Create a GPT Image 2 structured JSON prompt for carousel design"
---
<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

# /ai-carousel-carousel-design-prompt — Carousel Design Prompt

Thin wrapper — the full methodology lives in the skill.

## Steps
1. Load the spine: read `skills/ai-carousel-content-engine/genius.md` (patterns, signature moves, Anti-Patterns, Recognition Test).
2. Read and execute `skills/ai-carousel-content-engine/workflows/04-carousel-design-prompt.md` exactly as documented.
3. Run the workflow's Quality Gate before delivering.
