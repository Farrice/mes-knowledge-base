---
description: The Week-1 recipe, callable — receipts-built I-spine LinkedIn posts at the 8.5-proven standard: canon-grounded receipts, Lara body, Reader Contract, isolated verify + adversarial review, Ward saxon-punch line pass, felt-verdict loop
---

# /receipts-post — The Week-1 Production Recipe (proven 2026-07-21/22, Farrice-graded 8.5)

Reproduces end-to-end the pipeline that built the Week-1 launch posts. Pure conductor — every step is owned by an existing asset; this card only fixes the SEQUENCE and the standards. Never rebuild any layer here.

## When
Receipts-narrative LinkedIn posts (launch, story, proof posts) in Farrice's I-spine. NOT for deep-topic insight pieces — those run `/bayer-mirror` (content-class taxonomy in `directives/task-lifecycle-content.md` 2.5).

## The sequence (exactly as proven)

1. **Ground in canon — receipts only.** Load: `_active/farrice-brand/voice/VOICE-CARD.md` (dial BLEND) · `FARRICE-MASTER-CONTEXT.md` (§2 story, §8 receipts — every number/claim traces here or to shipped corpus) · the live avatar dossier + live offer doc under `_active/linkedin-launch/` (check for kill/supersede banners — offers die; the 07-21 audit died mid-week). Ejection-trigger list from the dossier is binding.
2. **Body: Lara I-spine.** `skills/lara-acosta-linkedin-mastery/` (SKILL.md + genius.md, patterns invisible — double-tap hooks, dwell, humble-brag routing). Production-sheet format: per post — hook + 2 variants, body, close type, receipt-it's-built-on line, first-comment copy. Content attracts; the door lives in the first comment.
3. **Gates, in order** (all exist in `directives/task-lifecycle-content.md`):
   a. `prose_classifier.py check` (cap 1 rewrite) → b. **Reader Contract** (Payoff / Pull / Recognition — any no = one revision) → c. **isolated fact-verify subagent** (every receipt vs canon; overturn verifier false-positives with fixed-string grep before accepting) → d. **adversarial voice review subagent** (fresh context; catches cross-post structural tells no scanner sees — the negate-reveal engine class).
4. **Ward line pass, surgical.** `/ward-saxon-punch` + `/ward-cadence-audit` on flagged lines ONLY — restraint law: ≤7 changes across a 3-post set, chord-change landings, never label the device.
5. **Felt-verdict loop.** Present takes; bank every Farrice critique via `voice_ratchet.py add` + taste ledger BEFORE editing; apply as targeted passes (payoff → tension → line), never full regens. His edits are calibration gold — diff and bank them.
6. **Close per the mission grammar**: deliverable to `_active/linkedin-launch/03-content/`, finalize with `--receipt`, posting protocol header (20 min engage before, every comment 30-60 min, can't engage = don't post).

PoC: `_active/linkedin-launch/03-content/WEEK1-POSTS-2026-07-21.md` (v4 — three posts, Farrice-graded strong-7s → 8.5s through this exact loop).
