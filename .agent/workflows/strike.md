---
description: JCC Strike Team — 2-4 coordinated experts
---

# /strike — JARVIS Command Center Strike Team

Force STRIKE TEAM scale — 2-4 expert sub-agents working in parallel on a focused mission.

## Execution

1. Read the JCC orchestrator skill at `~/.claude/plugins/installed/jarvis-command-center/skills/orchestrator/SKILL.md`
2. Read `evolution/failure-registry.md` at `~/.claude/plugins/installed/jarvis-command-center/evolution/failure-registry.md`
3. Skip intent classification — the user has explicitly chosen Strike Team scale
4. Run Step 2 (Complexity Scoring) for workstream design
5. Run Step 3 (Expert Selection) — select 2-4 experts from Google Antigravity roster
6. Run Step 4 (Deployment Proposal) — present the mission assessment
7. On user approval, execute per the STRIKE TEAM execution path in Step 5
