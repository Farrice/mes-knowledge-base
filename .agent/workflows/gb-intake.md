---
description: "/gb-intake — Client intake → free personalized mini-report inside the 48h promise: reads the frozen-shape intake-pack, seeds the radar from their own answers, bakes the mini, stages a Gmail DRAFT (never sends). Paid chain fires only on Farrice's explicit call."
---
<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

# /gb-intake — Growth Blueprint OS — Intake → Free Mini (manual fire)

Thin wrapper — the full methodology lives in the skill.

## Steps
1. Load the spine: read `skills/growth-blueprint-os/genius.md` (patterns, signature moves, Anti-Patterns, Recognition Test).
2. Read and execute `skills/growth-blueprint-os/workflows/gb-intake.md` exactly as documented.
3. Run the workflow's Quality Gate before delivering.
