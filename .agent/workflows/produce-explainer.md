---
description: "/produce-explainer — The full production cycle. Every step names its artifact; nothing ships without the QA loop."
---
<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

# /produce-explainer — Produce Explainer — annotated script → shipped 3-format package

Thin wrapper — the full methodology lives in the skill.

## Steps
1. Load the spine: read `skills/video-studio/SKILL.md` (methodology, standards).
2. Read and execute `skills/video-studio/workflows/produce-explainer.md` exactly as documented.
3. Run the workflow's Quality Gate before delivering.
