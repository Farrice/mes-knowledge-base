---
description: "/plugin-forge — /forge plugin <skill / prompt-set / workflow family> — Plugin Forge lane: a proven asset family → an installable Claude Code plugin. HARD-GATED: builds nothing without a verbatim operator lift token + 4/4 lift-plan fixtures passing in one run."
---
<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

# /plugin-forge — Plugin Forge — Proven Asset Family → Installable Plugin

Thin wrapper — the full methodology lives in the skill.

## Steps
1. Load the spine: read `skills/forge-os/genius.md` (patterns, signature moves, Anti-Patterns, Recognition Test).
2. Read and execute `skills/forge-os/workflows/plugin-forge.md` exactly as documented — honoring the execution prompt `skills/forge-os/references/prompts-v2/plugin-forge.md`.
3. Run the workflow's Quality Gate before delivering.
