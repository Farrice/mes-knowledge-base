---
description: "full character identity pipeline: face lock → outfit base → 3-panel sheet (strict Mode 0→1→2A order)"
---

# Character Lock (Joey — Noisy Group)

full character identity pipeline: face lock → outfit base → 3-panel sheet (strict Mode 0→1→2A order)

## Steps
1. Load the Joey spine: read `skills/joey-cinema-os/SKILL.md` (pipeline order + surfaces + gates) and `skills/joey-cinema-os/genius.md` (judgment layer + Anti-Patterns + rubric).
2. Read and execute the full workflow at `skills/joey-cinema-os/workflows/character-lock.md` exactly as documented (Pre-Flight, Steps, Output Format, Quality Gate).
3. Run the Quality Gate (`genius.md` § Anti-Patterns + § Quality Rubric) before delivering.
