---
description: Donald Miller — Message-Siloing System (one message + one specialized team per segment; demand + supply silos)
---

# Message-Siloing System

Split one overloaded message into segment-specific silos, each with its own pain point, vernacular, and specialist sales team.

## Steps
1. Load: `skills/donald-miller-messaging-evolution/genius.md`
2. Load workflow: `skills/donald-miller-messaging-evolution/workflows/05-message-siloing-system.md`
3. Execute: map distinct segments (by pain, not demographic) → write pain per silo → one message per silo → specialize the team → add supply-side silo if two-sided
4. Produce: silo matrix (segment → pain → message → specialist → channels) + team-split recommendation + cross-bleed flags
5. Finalize via chain_runner.py
