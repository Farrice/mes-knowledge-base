---
description: "/workflow-forge — /forge workflow <concept> — Workflow Forge lane: repeated manual loop or bare orchestration concept → a real `.agent/workflows/<name>.md` command with gates and verification inline."
---
<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

# /workflow-forge — Workflow Forge — Manual Loop → `.agent/workflows/<name>.md`

Thin wrapper — the full methodology lives in the skill.

## Steps
1. Load the spine: read `skills/forge-os/genius.md` (patterns, signature moves, Anti-Patterns, Recognition Test).
2. Read and execute `skills/forge-os/workflows/workflow-forge.md` exactly as documented — honoring the execution prompt `skills/forge-os/references/prompts-v2/workflow-forge.md`.
3. Run the workflow's Quality Gate before delivering.
