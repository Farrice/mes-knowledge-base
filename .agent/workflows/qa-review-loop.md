---
description: "/qa-review-loop — No editor edits with their eyes closed. Visual stages fail by default; this loop is where"
---
<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

# /qa-review-loop — QA Review Loop — render → picky editor → fixlist → revise (max 3 passes)

Thin wrapper — the full methodology lives in the skill.

## Steps
1. Load the spine: read `skills/video-studio/SKILL.md` (methodology, standards).
2. Read and execute `skills/video-studio/workflows/qa-review-loop.md` exactly as documented.
3. Run the workflow's Quality Gate before delivering.
