---
description: JCC Solo — single expert deployment
---

# /solo — JARVIS Command Center Solo Deployment

Force SOLO scale — direct handling with 0-2 expert personas from the Antigravity roster.

## Execution

1. Read the JCC orchestrator skill at `~/.claude/plugins/installed/jarvis-command-center/skills/orchestrator/SKILL.md`
2. Read `evolution/failure-registry.md` at `~/.claude/plugins/installed/jarvis-command-center/evolution/failure-registry.md`
3. Skip intent classification — the user has explicitly chosen Solo scale
4. Run Step 2 (Complexity Scoring) for informational purposes only — Solo is locked
5. Run Step 3 (Expert Selection) — select 1-2 experts from Google Antigravity roster
6. Run Step 4 (Deployment Proposal) — present the mission assessment
7. On user approval, execute per the SOLO execution path in Step 5
