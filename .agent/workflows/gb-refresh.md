---
description: "/gb-refresh — Staleness + drift sweep: reads manifest.json, checks pack freshness and artifact TTLs, detects content drift (upstream changed after downstream produced), and emits a drift report with the exact refresh order and commands. Flags, never blocks."
---
<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

# /gb-refresh — Growth Blueprint OS — Refresh (Staleness + Drift Sweep)

Thin wrapper — the full methodology lives in the skill.

## Steps
1. Load the spine: read `skills/growth-blueprint-os/genius.md` (patterns, signature moves, Anti-Patterns, Recognition Test).
2. Read and execute `skills/growth-blueprint-os/workflows/gb-refresh.md` exactly as documented.
3. Run the workflow's Quality Gate before delivering.
