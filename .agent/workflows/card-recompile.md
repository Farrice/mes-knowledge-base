---
description: "/card-recompile — When `python3 execution/voice_ratchet.py status` reports 5+ pending entries since the last"
---
<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

# /card-recompile — Voice Card Recompile

Thin wrapper — the full methodology lives in the skill.

## Steps
1. Load the spine: read `skills/voice-os/genius.md` (patterns, signature moves, Anti-Patterns, Recognition Test).
2. Read and execute `skills/voice-os/workflows/card-recompile.md` exactly as documented.
3. Run the workflow's Quality Gate before delivering.
