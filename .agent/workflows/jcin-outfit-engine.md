---
description: "outfit builds and swaps: Mode 1A/1B fork, Mode 5 two-ref swap, technical-flat ingestion"
---

# Outfit Engine (Joey — Noisy Group)

outfit builds and swaps: Mode 1A/1B fork, Mode 5 two-ref swap, technical-flat ingestion

## Steps
1. Load the Joey spine: read `skills/joey-cinema-os/SKILL.md` (pipeline order + surfaces + gates) and `skills/joey-cinema-os/genius.md` (judgment layer + Anti-Patterns + rubric).
2. Read and execute the full workflow at `skills/joey-cinema-os/workflows/outfit-engine.md` exactly as documented (Pre-Flight, Steps, Output Format, Quality Gate).
3. Run the Quality Gate (`genius.md` § Anti-Patterns + § Quality Rubric) before delivering.
