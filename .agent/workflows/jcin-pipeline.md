---
description: "end-to-end AI cinema mission conductor: bible → asset locks → scene plates → costed shot plan → shot prompts, checkpointed at every stage"
---

# Pipeline (Joey — Noisy Group)

end-to-end AI cinema mission conductor: bible → asset locks → scene plates → costed shot plan → shot prompts, checkpointed at every stage

## Steps
1. Load the Joey spine: read `skills/joey-cinema-os/SKILL.md` (pipeline order + surfaces + gates) and `skills/joey-cinema-os/genius.md` (judgment layer + Anti-Patterns + rubric).
2. Read and execute the full workflow at `skills/joey-cinema-os/workflows/pipeline.md` exactly as documented (Pre-Flight, Steps, Output Format, Quality Gate).
3. Run the Quality Gate (`genius.md` § Anti-Patterns + § Quality Rubric) before delivering.
