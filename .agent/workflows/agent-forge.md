---
description: "/agent-forge — /forge agent <concept or skill> — Agent Forge lane: an existing skill (or a raw concept that resolves to one) → agents/<name>/ deployable agent with a /[name] front-door command."
---
<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

# /agent-forge — Agent Forge — Skill → `agents/<name>/` Deployable Agent

Thin wrapper — the full methodology lives in the skill.

## Steps
1. Load the spine: read `skills/forge-os/genius.md` (patterns, signature moves, Anti-Patterns, Recognition Test).
2. Read and execute `skills/forge-os/workflows/agent-forge.md` exactly as documented — honoring the execution prompt `skills/forge-os/references/prompts-v2/agent-forge.md`.
3. Run the workflow's Quality Gate before delivering.
