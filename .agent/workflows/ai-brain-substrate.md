---
description: Install the second-brain substrate that makes the AI Brain Context Layer compound
---

# /ai-brain-substrate — Second-Brain Substrate Install

Bridge Business DNA into a live self-improving substrate — Kieran Flanagan's 5 building blocks + tuned writing logic on Simon's raw/wiki. This is **Workflow 05** — turns the static Context Layer into a compounding, sellable second brain.

## Prerequisites

A completed Discovery from `/ai-brain-discovery` (Business DNA). A Context Layer from `/ai-brain-context` is ideal but optional (the vault is created here if absent).

## Steps

### 1. Load Skill Context
// turbo
Read in order:
1. `skills/liam-mley-ai-brain-builder/genius.md` (esp. § The Second-Brain Substrate Bridge)
2. `skills/liam-mley-ai-brain-builder/workflows/05-second-brain-substrate-install.md`
3. `skills/simon-intellectual-library-os/genius.md` (§ Two Substrates + § Lifecycle) and `skills/simon-intellectual-library-os/workflows/library-second-brain.md`

### 2. Load Agent Persona
// turbo
Read `agents/liam-mley/AGENT.md` and embody the Liam Mley persona throughout.

### 3. Locate Discovery / Context Output
// turbo
Check `_active/ai-brain/` for existing Business DNA + Context Layer files. If found, use as input. If not, ask for equivalent business context. No DNA = stop (writing logic would be tuned on guesses).

### 4. Execute Substrate Install
Follow `workflows/05-second-brain-substrate-install.md` exactly — the 5 building blocks (vault · AI-reader · raw+wiki folders · tuned writing logic · connectors+habit), then run `/library-second-brain` and `/library-ingest-triage` on the wired vault. Honor the Execution prompt at `skills/liam-mley-ai-brain-builder/references/prompts-v2/second-brain-substrate-install.md`.

### 5. Deliver Output
Present the deployed vault + tuned writing-logic file + stakeholder/dependency graph + connectors + ingest habit trigger + one demonstrated ingest→triage→wiki round-trip + honest day-1/day-100 expectation. Next step: team/company tiers via `/library-brain-ladder`; pre-sale audit of a prospect via `/library-retrieval-audit`.

### 6. Save Output
Save to `_active/ai-brain/[client-or-business-name]/substrate/`.
