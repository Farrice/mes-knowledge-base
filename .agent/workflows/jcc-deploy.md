---
description: JCC Full Deploy — maximum parallel expert execution
---

# /jcc-deploy — JARVIS Command Center Full Deploy

Force FULL DEPLOY scale — 4-5 council-mapped Agent Team leads with expert sub-agents. Maximum parallel execution for complex missions.

## Execution

1. Read the JCC orchestrator skill at `~/.claude/plugins/installed/jarvis-command-center/skills/orchestrator/SKILL.md`
2. Read `evolution/failure-registry.md` at `~/.claude/plugins/installed/jarvis-command-center/evolution/failure-registry.md`
3. Read `config/token-budgets.md` at `~/.claude/plugins/installed/jarvis-command-center/config/token-budgets.md`
4. Skip intent classification — the user has explicitly chosen Full Deploy
5. Run Step 2 (Complexity Scoring) for full workstream design
6. Run Step 3 (Expert Selection) — cross-council selection from Google Antigravity roster
7. Run Step 4 (Deployment Proposal) — present the mission assessment with token budget warning
8. On user approval, execute per the CAMPAIGN / FULL DEPLOY execution path in Step 5
