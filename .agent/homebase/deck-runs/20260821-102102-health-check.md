# Deck run · /health-check

- model: sonnet · effort: low
- started: 2026-08-21T10:21:02 · duration: 104s
- measured cost: $0.8991

---

**Overall: WORKING, safe to use.** Nothing broken. Two things want your attention.

**ACTIVE and healthy**
- Harness, routing (553 decisions), Operator Core (14/14 aligned), Sovereign Memory (6,181 memories, backed up 10h ago), Performance Log, Session State.

**READY but not fired**
- Skill Evolution (Phase 2) — has 100+ log entries, never run. `/skill-evolution` to start.
- Gap Detection (Phase 4) — same, monthly cadence.
- Cross-Pollination (Phase 3) is BLOCKED behind Skill Evolution.

**Needs a look (DORMANT / drifted, none blocking)**
- `pulse-serve` routine exiting with code -15 — check `.agent/pulse/serve.log`.
- 5 launchd jobs gone silent past their cadence: angle-map-listening (13d), cos-notify (22d), shelf-report (never logged), social-pulse-farrice (10d), zeitgeist-daily (5d). These self-heal loops may be dark.
- Hook config drifted from the blessed baseline — `diff .claude/settings.json` against `.agent/health/hooks-baseline.json`.
- 2/85 verifiers failing (last run 2026-08-08).
- 25 distilled memories waiting on your review — `python3 execution/memory_review.py list`.
- Housekeeping backlog: `.tmp` at 12GB/200k files, 47 stale remote branches, 2,322 files >60d old in `_active/`, session context injection up 23% in a month.

**One safe next action:** run `python3 execution/memory_review.py list` — quickest unblock, 25 items, zero risk, and it's the one thing actively waiting on a decision only you can make. The launchd silences are next-tier — worth a `launchctl list <job>` pass but not urgent.

I did not write anything, sync Notion, or touch Mission — per the workflow, this was read-only.