---
thread: joey-cinema-os
status: ready
resume_hint: First live run: /jcin-product-lock on MyBPM hero product via Higgsfield MCP, costed via /jcin-shot-plan
unfinished: Farrice-judged blind pass for A-tier; prompts-v2 backfill for the 3 sibling production skills
branch: main
pin: true
---

# Joey Cinema OS — Forge Extraction + Install (3 skills + /jcin-* pipeline)

## Purpose
- **Next session should do:** first live production run of the Joey pipeline — `/jcin-character-lock` (a house test character) or `/jcin-product-lock` (MyBPM hero product) executed on Higgsfield MCP, banking outputs as reference elements; then a Farrice-judged side-by-side blind pass for A-tier promotion.
- **Not in scope:** re-extracting or editing the three installed production skills' locked blocks (they are verbatim Joey artifacts — extend never rebuild); re-running the harvest (all sources archived on disk).

## Load First
- `skills/joey-cinema-os/SKILL.md` — front door: pipeline order, 12-workflow table, stacking guide, surfaces/gates
- `skills/joey-cinema-os/genius.md` — the 24 patterns, signature moves, rubric, recognition test
- `.claude` memory `project_joey-cinema-os.md` — standing doctrine + critical GPT-2 disambiguation
- For a product run: `skills/joey-cinema-os/workflows/product-lock.md` + prompts-v2 `product-identity-lock.md`
- For a character run: `skills/joey-cinema-os/workflows/character-lock.md` + prompts-v2 `character-identity-lock.md`

## Current State
- **Objective:** cinema-grade, product-grade AI image/video via consistency infrastructure (bibles → identity locks → plates → block-structured video prompts).
- **What is already done (2026-07-13):** 3 videos frame-harvested via /watch + Notion doc captured; Joey's Skill Files v3.0 installed verbatim with install fixes (flat-grade remnants, save path, cross-refs); `joey-cinema-os` forged — 12 workflows, 8 born-v2 prompts (renaissance audit 0 fail), agent `joey-noisy-group`, all registries synced; routing wired (creative_router, routing-bindings, higgsfield-creative-studio, fantastic-posters/studio, gpt-image-2-director cross-pointer, usage policy); blind pass PASS (EVAL-035, model-judged), heartbeat 6/6, chain finalize logged (9/8/8, FG 9).
- **What is uncertain or stale:** A-tier promotion pending Farrice-judged blind pass; the three sibling production skills have no prompts-v2 of their own (backfill queue); credit figures beyond verbatim anchors are frame-read LIKELY (see `skills/joey-cinema-os/references/source-ledger.md`).
- **Latest proof/receipt:** `extractions/joey-cinema-os/blind-pass-log.md` (EVAL-035), Notion finalize log, heartbeat `skill_auditor.py check --skill joey-cinema-os` → 0/6 failing.

## Suggested Skills / Workflows
- `/jcin-pipeline` — end-to-end conductor if starting a whole world (asks the Existence Question first)
- `/jcin-product-lock` — the KY technical-flats method on a MyBPM product (recommended first live run: real client value + tests the product transfer)
- `/jcin-shot-plan` — cost the run before generating (cost gate still fires at execution; that's correct behavior, work with it)
- `/jcin-prompt-doctor` — if any generation drifts or a prompt bloats mid-run

## Exact Next Prompt
```text
/jcin-product-lock — MyBPM hero product. Ingest whatever product documentation exists in the MyBPM project folder (photos, spec notes, brand palette), build the palette sheet with colors-to-avoid, then produce the hero-angle lock and turnaround sheet prompts. Cost the run with /jcin-shot-plan before generating anything on Higgsfield MCP. Bank approved outputs as Higgsfield reference elements. When the turnaround is approved, show me the generated sheet next to Joey's amber-PVC corpus piece for my judged blind pass (A-tier decision).
```

## Acceptance Criteria
- One product (or character) identity locked: approved hero/face lock + turnaround/3-panel sheet generated on Higgsfield, banked as reference elements, total credit spend within the pre-approved shot plan.
- Farrice records a judged PASS/FAIL via `blind_pass.py record` (A-tier decision made either way).

## Risk Notes
- GPT-2 naming collision: Joey's "GPT-2" = Higgsfield GPT-2 (faces) ≠ OpenAI GPT Image 2 (`gpt-image-2-director`, weak faces). Wired everywhere, but stay alert in prompts/routing prose.
- Never add lighting to flat gray plates and never "fix" end-position negation blocks into positive phrasing — the flatness and the closers are the features.
- Higgsfield generation costs real credits — every run goes through `/jcin-shot-plan` + the cost gate; seedance-1080p via Fal stays hard-blocked.
- No secrets in this handoff; all referenced artifacts are repo paths.
