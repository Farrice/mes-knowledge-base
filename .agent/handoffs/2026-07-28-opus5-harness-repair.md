---
thread: opus5-harness-repair
status: ready
resume_hint: Send the 3 teardown DMs (copy in teardowns/ §8), log them, THEN clean-session About verdict
unfinished: Zero sends logged ever; About v11 unjudged in clean context; Fable A/B pending if it fails
branch: main
pin: true
---

# Opus 5 Harness Repair + LinkedIn Teardowns — Send Day (teardown DMs · About verdict · Fable A/B)

## Purpose
- **Next session should do:** (1) Send the three teardown DMs — copy is paste-ready in each teardown file §8 — and log each with `revenue_tracker.py` (Farrice logs sends himself; do not log for him). (2) In a CLEAN session, run `/writers-room` on About v11 under the new lens-card loading protocol and let Farrice judge it cold against v10. (3) If still not shippable, run the blind A/B: same draft, same loaded room, Fable vs Opus 5, unlabeled.
- **Not in scope:** New headline variants (iteration brake: two rejected takes already logged — go back to source, never a third variant). New assets of any kind before a send is logged. Re-opening the compass/gate work. Rebuilding anything harness-side.

## Load First
- `.agent/handoffs/2026-07-27-positioning-before-headline.md` — why headline work is STOPPED; the failure post-mortem; what is settled vs open
- `_active/linkedin/03-launch/teardowns/` — 3 teardowns, each with publishable post (§7) + send-ready DM (§8); publish dates 07-30 / 08-04 / 08-06, DM-first rule with 72h lead
- Scratchpad About v11 is ephemeral — the current About candidates live in `_active/linkedin/03-launch/2026-07-27-profile-and-content-package.md` (§3 = v9 register reference) and the v10/v11 work described in the 07-27 session; re-derive v11 via the room if the scratchpad is gone
- `FARRICE-MASTER-CONTEXT.md` — canonical before ANY identity/voice/offer writing (§10.4: producing documents instead of contact = say so once)
- `_active/farrice-brand/voice/VOICE-CARD.md` v1.1 — §4 spine choice, §6 imperfection principle
- `docs/solutions/2026-07-27-expert-load-truth.md` — the laundered half-pass failure + the fix contract (lens cards, firing rule, grep telemetry)

## Current State
- **Objective:** First sends EVER logged, then a clean-context taste verdict on the About.
- **What is already done:** 3 teardowns built from live surface (45+ verbatim quotes, prose-gate CLEAN). Profile package top-to-bottom (6 blanks only Farrice can fill; booking URL is the one section-blocker). Harness: compass doctrine (only cost gate + factual veto block), Opus 5 dialect block in CLAUDE.md, expert-load truth (grep→`skill_grepped`, dynamic room-roster nudge in finalize, live-tested), 15 lens cards compiled full-fidelity by fresh-context agents, `/writers-room` + `/content-sprint` loading protocols + Layer-1 checkpoint, co-creation Step 0 hook-injected (fires on taste/foggy, silent on execute). All committed & pushed through `be3f8286e`.
- **What is uncertain or stale:** Whether Opus 5 output passes Farrice's taste bar even with a truly loaded room — unproven; this session was 18 exchanges of mixed work, the worst context for judging it. The teardown DM recipients (names) were never researched — find the brand/growth person per company before sending. Old positioning docs (alignment-audit dossier, MARKET-ICP-DOSSIER, invisible-expert profile) are DEAD per Farrice — don't cite them.
- **Latest proof/receipt:** Finalize passed 8.0 (teardowns+profile) and 8.33 (harness); expert-load truth nudge fired correctly on a simulated 1-of-15 run; co-creation hook live-tested 3 ways.

## Suggested Skills / Workflows
- `/resume opus5-harness-repair` — reload this thread by name
- `/writers-room` — now card-based; watch for the EXPERT-LOAD TRUTH line in finalize as proof the room actually loaded
- `/cos` — morning brief queued (5 questions, 6 check-ins due)
- `python3 execution/revenue_tracker.py log` — Farrice logs his own sends

## Exact Next Prompt
```text
Send day. Open _active/linkedin/03-launch/teardowns/ and give me the three DMs verbatim with the best-guess recipient (name + title) researched per brand. I paste and send, I log them myself. No new assets, no edits to the teardowns unless I flag a factual error. After I confirm sends are logged: fresh-session /writers-room pass on the About under the new lens-card protocol — show me the load-truth line first, then the treatment, then I judge cold.
```

## Acceptance Criteria
- 3 sends logged in revenue_tracker (by Farrice) — the first ever
- /writers-room run whose finalize shows ≥9 skill_loaded and NO expert-load-truth warning
- A felt verdict (PASS/FAIL, his words) on About v11 recorded via /voice-ratchet

## Risk Notes
- Session-mixing is the documented quality killer — do NOT run the About judgment in the same session as the sends/research (session-management-protocol).
- Privacy law: family specifics never public; teardown claims must stay quote-anchored to the brands' own pages (re-verify any claim older than the 07-27 pull if publishing slips past 08-06).
- Cost gate untouched; no paid APIs needed for any of this.

## Do NOT Rebuild (auto-scaffolded — the store adds this when a handoff omits it)
- (first handoff on this thread — list shipped assets here as they land)
- Before building anything named above: `/arsenal <task>` and read the prior handoff first. Re-solving shipped work is the #1 next-session failure mode.
