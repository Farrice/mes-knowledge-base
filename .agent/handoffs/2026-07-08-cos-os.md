---
thread: cos-os
status: active
resume_hint: Next action is revenue: build + send the Josh pack (_active/josh-katie-fitness/)
unfinished: interests.json hint tuning; weekly board in 4d; 15 revenue check-ins overdue
branch: main
pin: true
---

# Chief of Staff OS — Brief Provenance + Research Stack Repair (Gemini citations, Tavily floor)

**Date:** 2026-07-08 · **Thread:** `cos-os` · **Repo:** `/Users/farricecain/Google Antigravity`

## Next Session Focus (the one thing)

**Revenue execution: build the Josh questionnaire + first creative brief to send-today quality.**
- Context: `_active/josh-katie-fitness/` + open loops in `.agent/cos/journal/2026-07-08.md`
- Status: Josh dodged the budget question in person (7/8); $500/mo floor offered; the pack IS the ask — send work product, then frame the direct ask as start logistics ("If the $500 works, I can start Monday — just say go")
- Strategic frame (delivered as Mentor insight, Farrice received it well): the Josh pack is a **demonstration asset** — build once, swap the front page, reuse as the Michelle-tier warm-outreach opener and the "this is what I do with AI" proof
- Farrice's state: -$30 bank balance, determined, wants direct short revenue paths, explicitly rejects long-tail multi-week plans

## What This Session Shipped (all verified, do not rebuild)

1. **COS brief provenance upgrade** (`execution/cos_prep.py`): every section header links its source file, every question carries a `↳ from:` provenance line, goal pulse shows "reviewed Nd ago" instead of fake "on track", question engine now 3-floor/5-ceiling (extras only from real material). Farrice's binding feedback: "headless" brief lines = failure; he must be able to click through to any source.
2. **Mentor seat + Counsel Close** (`skills/chief-of-staff-os/genius.md` + `workflows/cos-daily.md` Step 3): every daily close = CEO line + one capability-expanding insight + one cognitive-load lift. "Logging without guidance = failed session" — his direct ask ("I thought I would be getting more mentorship"). COS is also *therapeutic* for him — protect the reflective feel.
3. **World Pulse repair**: `world_pulse_research.py` no longer writes "[RESEARCH NEEDED]" placeholders (zero sourced items → exit 1, no file) so `cos_prep.ensure_world_pulse`'s fallback to `world_brief.py` (Tavily floor, $0) actually fires. Today's pulse: 8 sourced items, honest receipt.
4. **Research stack: 3 defects found via live proof runs, all fixed**:
   - `execution/research.py` dispatcher: accelerator "success" below the source floor no longer blocks the native floor — floor runs, better result wins, receipt names the rescue (verified live: 19 sources/12 domains/$0 after rescue)
   - `execution/deep_research_client.py`: Google's Interactions API drifted — citations now arrive as inline `vertexaisearch.cloud.google.com/grounding-api-redirect/` links, not a citations array. Added inline-URL harvest + redirect resolution (verified: 65 wrappers → 32 real domains, replayed offline from saved response)
   - `execution/deep_research_engine.py`: 401 latches Perplexity off for the run (was 12×401 spam per topic); Tavily is the automatic search fallback inside `_search_and_extract`
5. **Solution Card**: `docs/solutions/2026-07-08-success-without-sources-blocks-fallback.md` — the twice-in-one-day pattern (success-without-sources blocks healthy fallback).

## Standing Facts (memory already updated — honor these)

- **Perplexity is DEAD by choice** — out of credits, NOT renewing ("I don't have any money for that right now"). Never propose buying credits. `~/.claude/.../memory/deep-research-system.md` updated.
- Research legs, all verified with receipts: Tavily floor ($0, automatic) · Gemini Deep Research (`--depth max`, Ultra-covered via GOOGLE_AI_STUDIO_KEY, cost-gated) · Claude WebSearch (in-session) · NotebookLM (100/mo).
- COS daily marked done (streak 2). Ken loop was killed with receipts — it was auto-promoted from a "maybe" in his own 7/7 words; real loop is "warm outreach blocked on a value artifact."
- Chain finalizes logged: 2 (System type, 7.25 composite each, Notion-logged).

## Suggested Skills

- `/go "<messy thought>"` or `/raw-intent-bridge` — Farrice uses these to open work; honor the packet's route
- `/cos` — daily is DONE for 7/8; next run routes to status. Tomorrow's brief auto-renders the new provenance format
- `luke-iha-copy-blocks` / `master-copywriter` (agent) — for the Josh creative brief itself
- `/extract-approach` — if the Josh pack cracks a reusable client-format, bank the card
- `feedback-client-content-production-format` memory — Josh pack should be labeled production cards, ≤2 pages, never prose blobs

## Deferred / Known-Open

- World-pulse `interests.json` query hints are generic — tune toward live threads for sharper items (flagged, not urgent)
- `research.py` receipt suggests "agent fan-out → ingest" for full swarm depth — exists, unused this session
- Weekly board (`/cos weekly`) due in 4 days
- 15 revenue check-ins overdue (oldest 6/27) — `/weekly-closeout` when convenient
