---
thread: jen-canvas
status: parked
resume_hint: Parked by Farrice 2026-09-02; resumes when her six inputs land
branch: worktree-scrapes-routing
pin: false
---

# Handoff — jen-canvas (PARKED 2026-09-02, Farrice's call)

Parked mid-session on Farrice's instruction: "park the whole Jen Canvas thing, focus strictly on the Scrapes skills and integration."

## State when parked
- Jen's sibling brand context is on main: `_active/clients/jen-listings/brand_context/` (voice-profile.md, icp.md, visual_refs/travel-moments — INSPIRATION ONLY, never yellow/orange, INTAKE-PROMPT-PACK.md sent to her) plus a `BRAND.yaml` (added tonight) that points her output under her client root and names her pens (Alyssa placement + Luke grip; one integrator; jen-as-herself veto).
- Her first run (`projects/00-social-content/2026-09-02/jen-priced-out/`, v1 + v2 with cover A/B) and the canvas source (`v2/canvas/`) are on main as a RECORD. Canvas: https://claude.ai/code/artifact/084b1bd6-f6b0-4a4a-b1dd-9ddb6d611fb1
- `scrapes_brand.py check jen --pool instagram-carousel` → tokens.json missing, no pool, `render path: brand-renderer` (valley-editions/editions.py). Her real Scrapes run (mkt-visual-identity Import from `valley-editions/DESIGN.md` → ssc-template-builder ×6 → Template Studio → 00-social-content on "still renting") waits on her six inputs.

## Open verdicts (his)
- v2 copy (cover A): 9 or not? Which frame is short?
- Whether her BRAND.yaml pens/never list matches his read (edit in place).

## Resume
`/social-carousel` with "Jen" in the ask now locks her brand automatically and stops at the check with a plain reason until her inputs land. Nothing else to do until then.

## Do NOT Rebuild (auto-scaffolded — the store adds this when a handoff omits it)
- (first handoff on this thread — list shipped assets here as they land)
- Before building anything named above: `/arsenal <task>` and read the prior handoff first. Re-solving shipped work is the #1 next-session failure mode.
