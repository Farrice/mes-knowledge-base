---
thread: style-vault
status: blocked
resume_hint: Collect Farrice's go/no-go on the Carter extension map; GO option 1 = three welds (carousel engine <-> vault)
unfinished: Welds not applied; board->token bridge unwritten; call-transcript loop parked at 10-calls trigger
branch: claude/vigilant-leakey-5f6293
pin: true
---

# Research: Luke Carter Channel Harvest - Extension Map Delivered

## Purpose
- **Next session should do:** Collect Farrice's go/no-go on the extension map. If GO on option 1, apply the three welds wiring `ai-carousel-content-engine` to the style vault. If GO on option 2, write the board→token bridge reference file. Nothing else was authorized.
- **Not in scope:** New extraction from Carter videos (overlap check proved the catalog's relevant material is already on disk); any change to the BINDING deck doctrine (cover carries style, deck carries brand tokens); videos 4/5 (app-productizing, Telegram CRM agent — ruled no-build).

## Remaining Priority
Farrice's go/no-go on the extension map is the single blocker. On GO option 1: apply the three welds (style-match pre-flight checks `style_vault.py list`; carousel interiors consume `icp-worlds/<segment>/bank/`; copy-density reference rule). On GO option 2: one board→token bridge reference file. PARK stays parked until 10 logged sales calls.

## Load First
- `../style-vault/skills/style-vault/SKILL.md` — five-layer doctrine the welds must respect (skill lives in the UNMERGED `style-vault` worktree lane; after merge it is `skills/style-vault/SKILL.md` in main)
- `skills/ai-carousel-content-engine/SKILL.md` — the weld target
- `skills/ai-carousel-content-engine/workflows/05-carousel-style-match.md` — currently routes to /mood-board and never checks the vault
- `../style-vault/skills/style-vault/workflows/03-bank-and-batch.md` — where the "consumers" section (bank → carousel interiors) lands

## Current State
- **Where the map lives:** delivered in-chat 2026-08-10 (thread name: style-vault), finalized to Notion ("Luke Carter channel harvest…")
- **Objective:** Extend style-vault from Luke Carter's wider catalog per Farrice's instruction; extension map for go/no-go BEFORE building.
- **What is already done:** Channel identified (@lukesbrave, 124 videos, AI-design pivot ~Jan 2026). Five videos watched transcript-only ($0 paid APIs, no frames). Overlap-checked against style-vault five-layer doctrine + all four workflows + three references + craft-map rows. Decisive find: the teased carousel video (_3SEUgRCXX0) was ALREADY fully extracted as `ai-carousel-content-engine` (2026-08-07, names the video as source) — but the two systems are not wired to each other. Map delivered with go/no-go menu; chain finalize run (Research, factual 9, PASS); bookkeeping committed on lane `claude/vigilant-leakey-5f6293`.
- **The menu as delivered:** (1) GO — three welds, ~3 file edits, $0. (2) GO-IF client work imminent — board→token bridge (generated brand board → exact hexes, closest Google fonts, bg-removed assets, one-paragraph guidance → design-contract file for new client brands). (3) PARK — call-transcript→ICP refinement loop; trigger = first 10 logged sales calls. (4) NO-BUILD — videos 4/5.
- **What is uncertain or stale:** Carter's "carousels directly in code" automation has NO public video (school-community only) — the house already built the in-code equivalent independently (deck.html + Playwright exemplar at `_active/linkedin/03-launch/carousels/assets/carousel-01/`). Carter's full-image-carousel lane conflicts with the deck doctrine for Farrice's own brand — surfaced as client-brand option only.
- **Latest proof/receipt:** Notion finalize log 2026-08-10; lane commit "chore(session): Luke Carter channel harvest — extension map delivered, finalize bookkeeping"; tree CLEAN.

## Do NOT Rebuild
- `ai-carousel-content-engine` (7 workflows) — already extracted from the carousel video; the gap is wiring, never re-extraction
- `style-vault` five layers, realism floor, midjourney-port — carried; Carter's craft was already ~70% in nick-st-pierre/rory-flynn
- Brand-brain/wiki (Ag7g9u6E4Ko) — architecturally this repo already (living docs, memory facade, citation_integrity ≈ wiki lint)
- In-code carousel rendering — deck.html + Playwright exemplar exists
- Telegram CRM agent — pattern decommissioned house-wide

## Suggested Skills / Workflows
- `/arsenal carousel style wiring` — confirm no third carousel surface exists before editing (standing rule before building anything)
- Extend `03-bank-and-batch.md` in place (LIVING doc; no new dated file)
- `/watch <id>` — only if Farrice asks for a second-pass harvest of the website/funnel design series (out of vault scope by default)

## Exact Next Prompt
```text
Go on option 1 of the Carter extension map (thread: style-vault). Apply the three welds: (1) ai-carousel-content-engine/workflows/05-carousel-style-match.md pre-flight checks `python3 execution/style_vault.py list --brand <x> --platform <y>` first — a verified card IS the style board, skip reference hunting when one exists; (2) style-vault/workflows/03-bank-and-batch.md gets a "consumers" section — carousel interior images pull from icp-worlds/<segment>/bank/ before generating fresh; (3) add the copy-density reference rule to style-match. Extend living docs in place, no new files, no re-extraction.
```

## Acceptance Criteria
- Both skills cross-reference each other by path; `05-carousel-style-match.md` names the vault check as step 1
- `03-bank-and-batch.md` names its consumers (carousel engine, cover-plate workflow) in one section
- Zero duplicated craft (welds reference, never restate, nick-st-pierre/rory-flynn/grace-liu)
- No change to deck doctrine or to any Midjourney-port content

## Risk Notes
- style-vault skill lives in an UNMERGED lane — part of the 16-stranded-commits alarm; safest order: merge the style-vault lane first, then weld in main (one writer per tree)
- ai-carousel-content-engine is in main — a weld touching both trees before merge would split ownership
- No paid APIs needed for any GO option
