# Skill System Contract — Seth Godin Problem-to-Action Loop

| Field | Contract |
|---|---|
| Source evidence | Public YouTube URL plus `metadata.json`, `transcript.vtt`, `transcript.txt`, `transcript_segments.json`, `video-context-ledger.md`, `frame-notes.md`, and `uncertainty-report.md` in this package |
| Objective | Turn a tangled stuck state into one truth-owned, reversible action with a useful feedback loop and repeatable habit. |
| Components | Existing `seth-godin-philosophy` owner; existing `seth-godin-marketing-mind` handoffs; new problem-to-action workflow; one born-v2 execution prompt; cold command bridge; timestamped source package; behavior-proof fixture |
| Step order | classify problem/situation -> locate root entanglement -> expose incompatible demands -> audit actual risk -> choose smallest reversible generous test -> define owned feedback -> convert the move into a decision-once habit |
| Inputs | Verbatim stuck statement, stakes, constraints, attempted fixes, feared downside, people affected, current measures |
| Outputs | Problem-to-Action Memo: classification, root post, truth statement, tradeoff, experiment, feedback owner/measure, habit, stop rule, handoff |
| Handoff summary | Pass only the memo, exact source paths, unresolved risk, and recommended downstream owner; never pass the full transcript into the next workflow. |
| Composition rule | `seth-godin-philosophy` is the function owner. `seth-godin-marketing-mind` contributes only premise/quit-depth when the memo exposes those needs. Other experts are skipped unless the final handoff names a domain-specific build. |
| Human checkpoint | Required before a test causes external outreach, publishing, spending, destructive change, major positioning change, or a fundamental SVA pivot. Skipped for local analysis and reversible artifact construction. |
| Validation | Source package checks; prompt audit; skill validation; command/workflow routing search; cold-start applied scenario; behavior delta review |
| Behavior-changing proof | Apply the new workflow to a realistic “keep refining, no buyer exposure” scenario and show the changed decision, experiment, measure, and stop rule. |
| Result surface | Readable Markdown extraction and proof artifacts; reusable cold slash route surfaced by routers. |
| Context policy | Keep existing Seth front doors hot. Keep this video package and new workflow cold/on-demand. Load transcript segments only for evidence disputes. |
| Reuse hook | Route “I’m tangled/stuck and need the real problem plus one move” to the new workflow; route broader premise interrogation to `/godin-lens`, quit decisions to `/gmind-quit-or-dip`, and positioning to `/strategic-clarity`. |
| Goal packet | Not required: this source does not change maintenance, cleanup, evolution, or self-modifying behavior. |
| Agentic engineering packet | Not required: this source does not change dependency, package, review-loop, context-engineering, or source-code authority. |

## Build-Shape Verdict

**Component workflow inside the existing `seth-godin-philosophy` skill.** The source is rich, but most ideas already exist across the Godin stack. A new skill would duplicate the owner; a reference-only addition would fail to compose the mechanics into action.

## Skill System Handoff: Evidence -> Build

- **Source evidence**: `video-context-ledger.md` and timestamped segments in this package
- **Component used**: `/source-command-extract-mastery` plus `/source-to-skill-system`
- **Output produced**: bounded additive workflow contract
- **Next input**: verified mechanics and overlap verdict
- **Validation**: source package present; existing command/workflow/expert/context routers run
- **Open risk**: external interview claims remain uncorroborated; the build must not rely on them
