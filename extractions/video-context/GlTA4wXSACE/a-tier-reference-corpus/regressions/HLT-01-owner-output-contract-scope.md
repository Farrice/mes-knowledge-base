# Regression Fixture HLT-01: A Single Post Must Not Masquerade as a Viral Engine Dossier

## Replay Evidence

Replay: `replays/HLT-01-pass-1.md`

The receipt selected:

> Production owner: `viral-social-content-engine`.

The worker then returned one LinkedIn post. At replay time, the selected owner's shipped Output Contract required a dossier with an idea bank, content buckets, ten-frame matrix, three executions, share analysis, and a 30-day calendar.

## Failure Class

- **Rubric:** domain fitness and practitioner usefulness.
- **Router gate:** the final asset must satisfy the selected owner's own Quality Gate.
- **Failure:** no explicit subset rule connected a router-supplied one-post exit condition to this broader existing owner.

## Expected Behavior

When `/shaan-story-deploy` explicitly requests one social asset, the existing social owner may return that bounded subset only when its workflow and prompt authorize subset execution. It must still preserve the dosage, evidence, platform, and fact-integrity gates, and it must not imply that a full dossier ran.

## Smallest Existing Owner

`viral-social-content-engine` workflow and exact v2 prompt.

## Preservation Lock

- Keep the existing route and full-dossier capability.
- Add no route, expert, platform adapter, or second body owner.
- Preserve the full dossier when that is the requested output.
- Change only handling of an exact narrow exit condition supplied by the router.

## Status

- Initial replay: `REPAIR`.
- Repair: the existing social owner now explicitly supports an exact router-supplied subset while preserving its full-dossier default.
- Bounded replay: `replays/HLT-01-repair-1.md` declared one post and named all omitted dossier components.
- Final status: `REPAIRED`.
