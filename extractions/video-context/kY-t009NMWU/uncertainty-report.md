# Uncertainty Report — kY-t009NMWU

## Evidence state

- `VERIFIED`: public video identity, creator, title, 2:16:30 duration, the visible reverse-funnel diagram, live Instagram profile/grid teardowns, visible Meta policy pages, health/wellness examples, and an eight-week sprint surface.
- `TRANSCRIPT-VERIFIED`: the offer-upstream framing; attraction as audience matching; profile as a route into more content and trust; the German-finance diagnosis around a low-ticket paywall, profile ordering, trust, and offer path; the eight-week small-group sprint; and the creator's own result caveat.
- `USER-SUPPLIED`: the exact named case metrics and the ten-skill blueprint. The full runtime transcript was available for review, but the `/watch` script emitted it to the run report rather than a persisted file, so unlocated exact figures retain this label.
- `UNAVAILABLE`: native captions. Both the normal `/watch` path and the documented `web_embedded` fallback reported no subtitles or automatic captions.
- `COMPLETED`: approved OpenAI Whisper fallback. Three audio chunks produced 2,088 timestamped segments under the user-approved $0.82 ceiling. The initial TLS attempt failed before upload; the certificate-corrected retry completed.
- `UNVERIFIED`: creator revenue, client revenue, causality, algorithm claims, shadowban claims, and the claim that this method will produce $20k+.

## Consequences

1. Do not describe an exact case figure as transcript-verified unless its timestamped segment was retained in the ledger; otherwise keep it `USER-SUPPLIED / SOURCE-REPORTED`.
2. Treat "shadowban" as a distribution-eligibility or reach-risk hypothesis. Direct the operator to Instagram Account Status and current policy sources.
3. Treat bio, hook, and content scores as decision aids, not calibrated conversion predictions.
4. Treat offer prices and revenue targets as hypotheses until buyer behavior and attributable revenue exist.
5. Do not claim that an agent scraped Instagram, Reddit, or private Insights unless a permitted connector receipt exists.
6. Do not publish, message, clone a voice, create external pages, or write to ManyChat without explicit approval.
