# Global Systems-Thinking SHADOW Canary

## Status

`ACTIVE_CANARY`. The overlay is globally available as quiet advisory intelligence. It remains `SHADOW`: no promotion, enforcement, hot routing, mandatory trace, score, schema, hook, or automatic task creation is authorized.

## Evidence Boundary

Receipts 02–04 are retrospective portability evidence. They do not count as prospective causal proof. This canary starts with:

- qualifying live missions observed: `0`
- prospective material-change receipts: `0`
- controlled blind mission classes completed: `3`
- blind repair retests completed: `1`
- post-repair negative controls passed: `6/6`
- premise-preservation stress controls passed: `1/1`
- rejected pilot controls retained: `1`
- safe-work blocks: `0`
- unnecessary questions: `0`
- rollback events: `0`

Do not prefill or infer future evidence.

## Event-Only Record Rule

Record a prospective receipt only when the overlay materially changes at least one decision: route, architecture, craft ownership, reusable default, review boundary, recovery plan, or maintenance decision.

No material decision change means no receipt. Never activate the overlay merely to satisfy the canary count, and never add a question, field, or visible framework trace to prove it ran. Reuse normal Orchestration and Delegation Receipt fields when those receipts already exist; this artifact is an evaluation record, not a runtime schema.

For an eligible event, record only the task identifier or retrieval label, task type, activation reason, baseline decision, material decision changed, native owner preserved, question and block counts, context loaded, framework leakage, remaining risk, and `KEEP`, `TIGHTEN`, or `ROLLBACK` verdict.

## Observation Window

Review the first five qualifying live missions across:

1. revenue or offer judgment;
2. taste-heavy creative or LinkedIn work;
3. repeated system failure or repair.

If fewer than three legitimate material-change receipts emerge, continue through at most ten qualifying missions. Evidence remains insufficient rather than being manufactured.

After three prospective receipts, run anonymized baseline-versus-overlay comparisons with blind labels. The overlay result must be preferred or materially clearer in at least two of three comparisons, with no loss of voice, taste, Preservation Locks, specialist depth, or viable creative range.

## Controlled Lift Evaluation (2026-08-09)

The initial blind round produced one treatment win, one tie, and one treatment
loss. The loss was material: the treatment bundled source validation,
regeneration, and ledger repair into one next action.

One targeted preservation invariant was added to protect material inherited
premises and the nearest reversible step. A frozen retest of the failed mission
then produced a treatment win. The final controlled result across the three
mission classes is `2 treatment wins`, `1 tie`, and `0 treatment losses after
repair`, with no treatment harm flags.

This passes the controlled 2-of-3 lift threshold. It does not count as a live
production receipt and does not change SHADOW status. Full evidence and the
rejected pilot control are recorded in `global-shadow-lift-test-round-01.md`.

## Negative Controls

The following must remain silent and unburdened:

- a tiny mechanical request;
- a clear single-owner task;
- divergent creative exploration before taste selection;
- a delegated task whose worker needs only a bounded decision delta.

Passing means no overlay trace, no extra question, no delay, no premature paved path, and no worker-side recursive activation.

### Deployment controls (2026-08-09)

- Tiny mechanical task: `PASS`. It returned only the arithmetic answer.
- Clear single-owner diagnosis: `PASS`. It returned one direct cause with no widening or question.
- Divergent creative exploration: `PASS`. It produced five unranked directions without selecting or standardizing them.
- Bounded delegated validation: `PASS`. It inspected only the assigned verifier slice, returned one concrete risk, and did not load the primitive or source package.

These are deliberate negative controls, not prospective mission receipts. The prospective counts remain zero.

### Post-repair controls (2026-08-09)

- Tiny mechanical task: `PASS`.
- Clear single-owner diagnosis: `PASS`.
- Divergent creative exploration: `PASS`.
- Bounded delegated worker: `PASS`.
- Factual veto with safe continuation: `PASS`.
- Permission boundary with no external action: `PASS`.
- Locked revenue-premise stress control: `PASS`.

One under-specified permission pilot is retained as `FAIL` because it invented
offer details while self-reporting success. The corrected allowed-facts control
and independent artifact audit passed. This repaired the evaluation method; it
did not add a runtime overlay rule.

## Kill Gates

Rollback the global pointer immediately if the overlay delays or blocks safe work, adds a question to a clear task, activates on tiny work, displaces the native owner, damages creative range or an approved primary, loads the transcript or deep extraction during ordinary work, or exposes framework theater without changing a decision.

Two consecutive no-delta activations trigger tighter activation, not another rule.

## Rollback Boundary

Rollback removes only the marker-bounded paragraph in global Codex instructions. The canonical primitive, source package, receipts, and Google-local SHADOW integrations remain intact. No canary result changes status automatically; any future status change requires explicit Farrice approval.

## Prospective Receipts

No prospective receipt has been recorded.
