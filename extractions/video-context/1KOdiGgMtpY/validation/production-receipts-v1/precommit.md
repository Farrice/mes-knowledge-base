# Signal Fidelity Production-Receipt Precommit

Status: `FROZEN BEFORE RECEIPT EXECUTION`

## Objective

Run the repaired recipient-aware SHADOW layer against three distinct real
workspace artifacts without changing, publishing, sending, promoting, merging,
or enforcing anything.

## Cases

1. Consequential strategy:
   `_active/linkedin/05-lead-gen/demo-proof-strategy.md`
2. Paired human and AI handoff:
   `.agent/handoffs/2026-08-29-offer-packaging.md`
3. Public-content owner-only audit:
   `_active/linkedin/04-deliverables/content-os/hiya-aeo-teardown/linkedin-post.md`

The three source hashes are frozen in `manifest.json`. Source artifacts are
read-only and may not be rewritten by Signal Fidelity.

## Measures

- **Recoverability proxy:** deterministic coverage of declared recipient,
  intended change, source of conviction, promise, limit, proof state, and
  must-survive markers. This is inspectable signal recovery, not proof of human
  comprehension.
- **Visible burden:** words added to the recipient-facing artifact, generated
  questions, and whether a second visible object is required.
- **Creative preservation:** the public draft hash must remain unchanged and
  Signal Fidelity must retain zero composition authority.
- **Proof restraint:** no evidence upgrade, false block, mutation, or claim that
  a local evaluator proves trust, adoption, revenue, or field comprehension.

## Independence Boundary

These are three independent artifact receipts, but the builder is not an
independent unfamiliar human recipient. Therefore:

- deterministic recoverability may be `VERIFIED`;
- independent human comprehension remains `NO EVENT`;
- production field effect remains `UNTESTED`;
- this run cannot authorize merge, promotion, enforcement, or automatic use.

## Stop Conditions

Stop after one receipt wave, the four-recipient demonstration, and verifier
replay. Any source-hash change, public-content mutation, generated question,
blocking authority, composition authority, or proof upgrade fails the wave.
