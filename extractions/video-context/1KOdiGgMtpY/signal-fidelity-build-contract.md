# Signal Fidelity SHADOW Prototype — Build Contract

## Skill System Contract

| Field | Prototype value |
|---|---|
| Source evidence | YouTube `1KOdiGgMtpY`; `transcript.vtt`, clean transcript, timestamped segments, ledger, uncertainty report, and analysis in this directory |
| Objective | Reveal material loss of human-owned intent, limits, proof state, and must-survive details across selected AI transformations without blocking work or adding a new control plane. |
| Components | New cold semantic primitive; new stdlib-only advisory evaluator; new verifier and fixtures; optional references from Co-Creative Launchpad, Skill System, and Agentic Engineering owners |
| Step order | acquire source → check existing routes → lock contract → build primitive → build evaluator → add owner pointers → run positive/negative/false-alarm tests → review diff → human checkpoint |
| Inputs | Optional Signal Contract packet, transformed artifact text, and optional cold-recipient playback text |
| Outputs | Advisory JSON/plain report with observed coverage gaps, possible distortion class, evidence, uncertainty, and human review question |
| Handoff summary | Pass the compact checksum, promise plus limit, must-survive markers, proof state, human owner, source path, and open risk—not the full source corpus. |
| Composition rule | Co-Creative Launchpad owns capture; Skill System owns propagation; Agentic Engineering owns playback/review. Signal Fidelity owns no upstream decision and may not override those owners. |
| Human checkpoint | Farrice must approve any promotion, mandatory field, automatic activation, routing change, hook, global mirror, or enforcement. Current approval covers only this reversible workspace-local SHADOW prototype. |
| Validation | Source-package verifier; dedicated positive/negative/false-alarm verifier; existing contract verifiers; control-plane and live-surface audits; clean diff inspection |
| Behavior-changing proof | Three fixtures: preserved signal, missing limit/proof inflation, and intentional adaptation that must not be called distortion automatically |
| Result surface | Concise conversation readout plus local source, contract, evaluator, fixtures, and verifier files |
| Context policy | Keep only a compact checksum hot on selected runs; load source evidence and full contract on demand; keep transcripts cold. |
| Reuse hook | Consider only for material, multi-transform, taste-bearing, public/client, source-derived, or delegated work. Skip tiny and clear tasks. |

## Agentic Engineering Packet

| Field | Prototype value |
|---|---|
| Objective | Produce an inspectable, non-blocking fidelity report that never claims semantic certainty. |
| Source truth | This source package; current Launchpad, Skill System, Agentic Engineering, Systems Thinking, and Nate B. Jones intent-engineering surfaces |
| Context plan | Primitive and compact packet on demand; exact source paths in handoffs; transcript and adjacent skills remain cold after build. |
| Work chunks | Source package; semantic contract; evaluator; three owner references; fixtures; verifier; regression suite. |
| Review loop | Maximum two repair passes. Pass requires dedicated verifier success, negative-control detection, false-alarm restraint, no forbidden activation surface, and existing contract checks. |
| Dependency gate | No new packages, services, model calls, network use at runtime, or external state. Python standard library only. |
| Structure pass | Inspect touched files for duplicated owners, schema creep, runtime authority, and unclear removal path. |
| Use-now artifact | `execution/signal_fidelity_shadow.py` run manually against a supplied packet. It is not routed or automatically invoked. |
| Hardening proof | Cold-start fixtures, missing-limit/proof-inflation detection, intentional-adaptation restraint, source package validation, and live-surface audit. |

## Goal Packet

| Field | Prototype value |
|---|---|
| `target` | Signal preservation across selected Co-Creative Launchpad, Skill System, and Agentic Engineering handoffs |
| `scope` | Additive SHADOW primitive, manual evaluator, fixtures, verifier, and three pointer-level owner references; excludes commands, routers, hooks, skills, agents, automations, global files, and enforcement |
| `per_item_criteria` | Every touched owner retains authority; evaluator reports evidence and uncertainty; negative controls detect missing limits and proof inflation; intentional adaptation remains reviewable rather than automatically failed. |
| `permitted_side_effect` | Add or edit only the named workspace-local prototype and owner-reference files in this Codex lane. |
| `proof_artifact` | `extractions/video-context/1KOdiGgMtpY/behavior-proof.md` plus dedicated verifier output |
| `measurable_stop` | Stop after all dedicated fixtures and existing-owner verifiers pass, forbidden activation surfaces are absent, and the diff contains no enforcement path. |
| `turn_cap` | Two implementation/repair passes before returning `PARTIAL` for human review. |
| `evaluator` | `execution/verify_signal_fidelity_shadow.py` plus existing control-plane audits and final human judgment |
| `wake_up_check` | `python3 execution/verify_signal_fidelity_shadow.py` |
| `human_checkpoint` | Current build is approved; promotion or automatic use is not approved and must pause. |
| `rollback_or_archive_rule` | Discard the branch or revert the additive commit. No migration or data rollback is required because the prototype writes no runtime state. |

## Evolution Council Verdict

- **Forager digest:** The source identifies three downstream distortion classes and recipient playback. Existing Antigravity contracts already own upstream intent, context, proof, and handoffs; the unique gap is post-transformation meaning recovery.
- **Build hypothesis:** One cold contract plus a manual advisory evaluator and three existing-owner pointers can expose the gap without adding a control plane.
- **Risk review:** The main risks are duplicate intent machinery, mandatory-schema creep, marker false positives, freezing improvable intent, and mistaking comprehension for truth or trust.
- **Change scope:** Source package, one primitive, one evaluator, one verifier, three fixtures, and pointer-level edits to three existing contracts only.
- **Evaluation plan:** Prove successful preservation, catch missing limits and proof inflation, decline to call intentional adaptation a confirmed distortion, and scan for forbidden hot surfaces.
- **Target:** Signal Fidelity SHADOW prototype
- **Goal packet complete:** yes
- **Recommended path:** self-evolve through the approved Source-to-Skill-System build, remaining SHADOW
- **Permitted side effect:** additive and narrow workspace-local edits in `codex/signal-fidelity-shadow`
- **Proof artifact:** behavior proof plus verifier output
- **Stop condition:** dedicated and existing-owner checks pass with no enforcement surface
- **No-regression check:** existing contract verifiers, system control-plane verifier, and strict live-surface audit
- **Human checkpoint:** required before promotion, automatic activation, or merge into a broader global surface
- **Open risk:** real recipient comprehension and workflow burden remain `UNTESTED` until live blind comparisons
