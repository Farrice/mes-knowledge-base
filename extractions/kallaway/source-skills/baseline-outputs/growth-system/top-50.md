# Top-50 Scan — Farrice Cain

*Produced by the Topic Scanner (step 4 of the 0→100K Growth System). This file is the shared data core the topic-brainstormer, format-finder, and both hook machines reuse.*

> **⚠️ SCAN NOT RUN — UNVALIDATED BY PERFORMANCE DATA.** Pulling the top 50 requires `search_my_videos` over the Sandcastles watchlist (~180-day lookback, sorted by outlier score — outlier is the right sort because it normalizes for channel size: a 5x from a 20K channel is a stronger pattern signal than an average video from a 2M channel). The MCP is not connected and no video list was pasted for the manual fallback. **0 of 50 rows exist.** The table structure below is the contract every downstream skill reads; it fills when the scan runs.

## The qualifies protocol (defined, waiting on data)

Every pulled video gets a visible verdict, never a silent drop:
- **✓** — fits the niche and the avatar (supplement/performance/DTC-health operators, aimed at Rings 2–4).
- **✗** — off-niche, wrong avatar, or Ring 5 generic breadth — with a two-or-three-word reason on every ✗ ("wrong avatar," "too broad," "off-niche"). ✗ rows stay visible, rendered ~~struck through and dimmed~~, kept in performance-rank position, so the "don't make this" calls read at a glance. Backfill from next-ranked candidates until ~50 rows carry a ✓.
- Videos in one of the **3 chosen buckets** get a ★ next to the bucket name.

## The table

| Rank | Qualifies | Topic bucket | Subject (one-liner) | Channel | Views | Outlier | Video link | Sandcastles analysis |
|---|---|---|---|---|---|---|---|---|
| — | — | — | *no rows: scan not run — no MCP, no pasted video list* | — | — | — | — | — |

**To fill this file:** connect the Sandcastles MCP (sandcastles.ai → Claude connector settings) and re-run the topic-scanner, or paste a list of top niche videos (links + rough view counts) and a degraded manual pass can classify them — still labeled unvalidated by performance data.
