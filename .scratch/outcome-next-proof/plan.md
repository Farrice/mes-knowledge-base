# Own the outcome through the next useful proof

Authorization: Farrice explicitly requested installation, testing, and verification on 2026-09-08. Main thread owns all edits. No subagents, paid tools, global configuration changes, or changes to other tasks.

Goal packet / engineering packet:
- Target: existing Codex hook runner; new stdlib companion module and focused verifier; approved text inside existing operating-alignment contract.
- Scope: UserPromptSubmit reminder and observe-only Stop review. Preserve existing hook outputs, decisions, target selection, approval gates, and tiny-turn silence.
- Failure evidence: Rosita task selected an interface before verifying operation access; invalid photo/prompt pairings preceded broad build rejection; outsourced app pricing did not compare an assistant-operated private tool. MES preflight proposed patching a read-only inquiry.
- Hypothesis: inject the approved outcome contract before meaningful requests, then flag bounded evidence gaps in the actual current turn. Do not equate tool counts with effort or NO_FLAG with quality proof.
- Permitted side effects: lane-local implementation, tests, receipts; guarded local integration. No purchases, external writes, global edits, cleanup of foreign work, or new hook registrations.
- Evaluation: real runner subprocess tests plus Codex/Claude transcript fixtures, listing failure controls, read-only and tiny-turn negative controls, error and kill-switch tests; existing Codex authority/operator checks.
- Proof artifact: this folder's verification receipt and execution/verify_outcome_next_proof.py.
- Stop: focused checks pass, existing relevant regressions assessed, integration attempted through lane helper, main installation verified if integration succeeds. Never label a parked lane installed.
- Turn cap: three implementation/review cycles; report remaining errors rather than broadening into unrelated repairs.
- Wake-up: python3 execution/verify_outcome_next_proof.py.
- Rollback: revert only the owned change through a lane; OUTCOME_NEXT_PROOF_OFF=1 disables the companion without changing existing targets.
- Human checkpoint: installation already authorized; foreign-work reconciliation requires a separate decision if dirty main blocks integration.
- Risk: deterministic text heuristics can miss nuanced failures or flag acceptable prose. They remain advisory REVIEW_REQUIRED, NO_FLAG, or UNOBSERVED, never a quality score or hard stop. Actual behavioral improvement requires a future real task.
