# Package Release — exports → publish-ready package (Farrice posts)

Distribution ruling (2026-08-06): the studio delivers everything needed to post;
the posting hand is HIS. No auto-publish wiring.

Deliver one folder `exports/release-<date>/` plus a forwardable summary containing:

1. **Files**: yt-169 export · vert-916 export (captions burned) · linkedin export (captions burned). Confirm all three passed qa-probe.
2. **Titles**: 3 options per platform — load a hook skill (attention-hijack-hooks / diandra hooks for LinkedIn, kallaway-hook for YT). Pick-one options, never homework.
3. **Description**: YT description (hook line + chapters from beats + links) · LinkedIn caption in his voice (VOICE-CARD + platform-narrative-card; I-narrative, reader-contract dials).
4. **Thumbnail**: via /generate + fantastic-studio (concept-first, not a frame-grab) — 2 options. Respect Premium Minimal brand.
5. **Hashtags/tags**: platform-appropriate, minimal, no slop.
6. **Post plan line**: when to post, in one sentence, per platform-pulse if fresh.

Close-out: Feedback Triad → corrections promoted to the brand style file → mark project.json shipped → manifest check. Prose passes `prose_classifier.py check` before delivery (ban bank).
