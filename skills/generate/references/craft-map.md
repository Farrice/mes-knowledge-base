# Craft Map — which master loads before which generation (BINDING, Farrice 2026-08-02)

**The rule: no generation — paid or free, draft or final — fires with a freehand prompt.**
Every prompt is authored through the matching craft layer below, then gets a doctor pass.
Scar this exists for: the 2026-08-02 Seedance teaser take 1 — prompt written freehand while
`cinema-worldbuilder-pro` sat extracted on the shelf; output was slop. Take 2 through the
grammar, same model, same price: production-usable. The knowledge was never the gap;
loading it mechanically is.

| Output | Load FIRST | Doctor pass |
|---|---|---|
| Seedance video (any) | `skills/cinema-worldbuilder-pro/SKILL.md` — mode select (M1–M5), Write-the-Visible, FOV table, block structure | Read the prompt back as the shot: every word renders a pixel or gets cut |
| Kling video / multi-shot | `skills/fantastic-posters/workflows/kling-multishot.md` + cinema-worldbuilder cut grammar | Cut list explicit, continuity held across cuts |
| AI-video direction / film craft | `/pj-accetturo-ai-video` · `/tao-prompts-ai-video` | Story beat before shot beat |
| Stylized image (catalog style) | `skills/fantastic-posters/SKILL.md` style picker + the style's own build notes | Style fields (title/subtitle/footer/subject) filled deliberately, never defaulted |
| Open art direction (direct lane) | `/art-direct` or `/creative-prompt` (creative-direction skill) + relevant style card from `skills/generate/styles/` | Prompt is a full production document: subject, composition, light source+direction+temp, lens language, palette anchored to surfaces, finish register |
| Photoreal people | `fal-people` lane via `creative_router.py` (BINDING; Higgsfield RETIRED 2026-08-06 — nano-banana-2 w/ `--reference` default, flux-2 escalation) + `/banana-pro` prompt grammar for character builds | Face/identity locked to reference, never freehand; anti-AI-slop anatomy paragraph appended |
| Persistent character/world | `/jcin-pipeline` (bible → locks → plates) + `jcin-prompt-doctor` | Canon checked against the bible |
| Anime/manga (his lane) | direct lane + his style cards (`manga-ink`, `shonen-ui-glow`, `alchemist-blaze`, …) + **the locked character: `frbpm` — canon + dataset + usage recipe at `_active/farrice-brand/05-assets/anime-avatar/CANON.md`** (always pass 2-3 dataset refs closest to the target shot; drift → mickmumpitz snip-and-refeed) | Register named (which anime grammar), text minimal and intentional; frbpm's LOCKED column (face, fade, facial hair, headphones w/ cyan ring) never re-described ad hoc — quote the CANON identity map |
| Vector/text-heavy (recraft) | recraft recipe notes + `/kittl` typography judgment for lockups | Text content exact, style param deliberate ($0.04 vs $0.08 vector) |
| Audio TTS/music | recipe notes; voice/tone brief written like a VO director's note (pace, register, where the emphasis lands) | Read the text aloud mentally — punctuation drives the read |
| Mood boards / visual development | `/mood-board` (creative-direction skill) | Board argues ONE direction, not a collage of maybes |
| **Image art direction — look undecided, aesthetic sweeps, style banks** (the pre-generation decision layer, model-agnostic) | `skills/nick-st-pierre/SKILL.md` — five pillars (layered build · controlled sweeps not rerolls · style-code library · reference-over-adjective · image-as-substrate); sweep = `workflows/01`, build = `workflows/02`, bank = `workflows/03`. Tool syntax is quarantined in `references/era-bound-mechanics.md` — verify before use | Run the ten-check critique pass. Two that fail most: is the light **named and placed** (not "beautiful lighting"), and **what is in tension** — an image where every element agrees with every other is the definition of slop. Then: would this have looked the same without me? |
| **Image OPERATIONS — many on-brand images, not one** (style-handle sweeps, style/moodboard libraries, brand-consistent asset systems, batch production graphs) | `skills/rory-flynn/SKILL.md` — nine Image Elements, frozen-backbone/variable-head split, then the matching workflow: `moodboard-sweep.md` (characterize handles: null run → solo→stack ladder → weight sweep → named recipes) · `style-code-library.md` (backbone + handle inventory + handoff signature) · `photorealism-language.md` (photography vocabulary, left-anchor swap, imperfection pass) | Two checks. **(1) No element silently delegated** — every one of the nine is decided or marked deliberately open; unspecified = model-chosen = drift. **(2) The handoff test** — "any \<input\> goes in, \<output\> comes out"; if only you can operate it, it isn't a system. Style handles never enter production uncharacterized (no null run = no card = don't ship it) |
| **Storyboard / previs / anything BEFORE the first generation** (narrative pieces longer than a clip, drifting characters or locations, "what do I build first", quoting a cost) | `skills/curious-refuge/SKILL.md` + `genius.md` — the shot-list floor, the **composition question** (board it only when the frame is load-bearing; brief it when only the idea of the shot is), the **reference-asset manifest** (character sheet per identity · **two angles per location** · style anchor · voice bed), then the matching workflow: `01-preproduction-package.md` (spine + manifest + generation budget) · `02-storyboard-to-plate.md` (three-channel assignment, CCR direction, specify-vs-cover, chain plan) · `03-voice-and-performance-direction.md` (voice rungs + the ADR seam). Tool layer quarantined in `references/era-bound-mechanics.md` — verify before use | Three checks. **(1) No channel silently delegated** — composition, identity, style and location each pinned to a *named* reference, or explicitly marked open. **(2) Never judge a prompt on one output** — batch first; *"the actual problem is just you didn't generate enough images."* **(3) Salvage before you reject** — half a second counts; a rejection must name the **channel** that drifted, never "switch models" or "reword it" |
| **Character consistency & previs — "it changed between shots", recurring character/mascot/anime lock, multi-character in one frame, camera that must repeat** (the determinism lane, model-agnostic) | `skills/mickmumpitz/SKILL.md` + `genius.md` — the Control Ladder (6 rungs: prompt → reference → mask → structure → tracking → 3D layout → trained character), the Four Building Blocks (mask=WHERE · driving plate+structure=WHAT SURVIVES · references=IDENTITY · prompt=WHAT HAPPENS), and the five dataset rules. Character lock = `workflows/01`, blocking/previs = `workflows/02`, controlled shot = `workflows/03`. Stack specifics quarantined in `references/era-bound-mechanics.md` — verify before use | Three checks. **(1) When it's wrong, author a better picture — never a better adjective**: every fix is an artifact (reference, mask, caption, geometry), and no finding may be answerable with "switch models". **(2) The honest-folder standard** — a character is locked when the WHOLE output folder holds, failures scrolled alongside successes, each signature detail checked individually; one good frame proves nothing. **(3) 80% is a symptom, not a result** — "works 80% and something's always weird" means an axis is still un-authored; go back to the four blocks and find which |
| Cinematic direction / "why does this look flat?" | `skills/dave-clark/SKILL.md` + `genius.md` — the eight causes of flat, look card (light / black point / palette / atmosphere / capture register), coverage pairs, cadence plan. Shot list BEFORE prompt; hybrid work → `workflows/02-hybrid-pipeline-plan.md` | Run the flat-to-cinematic audit: name one motivated light source, a real black point, atmosphere in the mid-ground, ≥5 takes composited, uneven cut rhythm, one global capture layer. **No finding may be answerable with "switch models."** |
| **3D camera control / greybox lane — the camera move has to be EXACT, or the same place must recur** (spatial-control layer: load when a camera move is load-bearing, a location/character appears in 2+ shots, a real place must be right, or text-to-video keeps returning a different move than the one asked for) | `skills/bilawal-sidhu/SKILL.md` + `genius.md` — the **HELD/BOUGHT split** (greybox holds geometry, scale, camera path, timing, occlusion · reskin buys material, light, atmosphere, register), the three control artifacts (**greybox render** · **annotated plan** — line, arrowhead, numbered waypoints, POI circle · **pose set/frustums**), the **reproducibility gate** (appears once → generate; 2+ → freeze the set first), **retrieve-don't-recall** grounding. Shot = `workflows/01-greybox-reskin-shot.md`, multi-shot piece = `workflows/02-scene-first-blocking.md`, single drawn camera artifact = `references/prompts-v2/camera-path-brief.md`. Tool layer quarantined in `references/era-bound-mechanics.md` — verify before use | Three checks. **(1) Nothing in both columns, nothing missing** — every property is HELD or BOUGHT; holding fog while prompting fog is paying twice, and unspecified is delegated, not neutral. **(2) What would I have LOOKED AT before generating that told me this was wrong?** — "nothing" means the shot was uncontrolled and the bad output wasn't bad luck. **(3) Grade path → geometry/scale → timing → look, in that order** — drift in the first three is a **control failure** (fix the artifact, never the prompt); a miss in look is a **brief failure**. Reskin briefs contain **zero camera language**; light is direction + named source, atmosphere is a named physical phenomenon (fog, caustics, god rays), never "cinematic" |
| **Taste doctrine — "is this slop, and at which layer?"** (runs ABOVE all craft rows; the direction-vs-prompting diagnosis, medium-agnostic — visual, video, content, campaign) | `skills/grace-liu/SKILL.md` + `genius.md` — the three layers (**Human** = intention originates · **Translation** = intent becomes machine-readable · **Machine** = systems execute), the five tests (authored / defaults / insulation / multiplier / articulation), the tier ladder (Prompt Operator → Aesthetic Stylist → World Architect), the travel test. Rubric skill, no workflows: diagnose = `references/prompts-v2/direction-discipline-audit.md`, brief BEFORE generating = `references/prompts-v2/human-layer-brief.md` | Ladder rule — **you may not diagnose at a lower layer until the one above passes**, and naming failures at all three layers has diagnosed nothing. Three gates: **(1)** answer *"what are you reframing, and for whom?"* — unanswerable ends the audit, no craft fix reaches it. **(2)** Name what is **in tension**; if every element agrees, there is a style and no idea. **(3)** Count the **silent delegations** — every element you didn't name was decided by the model's defaults. Then: name three invisible planning decisions, or the work is mass-produced at any budget. Chain: Liu Human gate → Clark causes 1–7 → St. Pierre / Flynn for translation craft |
| **Fashion / streetwear CAMPAIGN — a coherent SET, not one frame** (drops, lookbooks, editorials, product campaigns; **My.BPM is the house use case**) | `skills/fashion-coupids/SKILL.md` — the seven-link chain (intent → culture → brand → **one-sentence metaphor concept** → loose board → shot list + real-life check → generate/composite/unify/place) and the four-move spine **RESEARCH → GENERATE → COMPOSITE → UNIFY**. Chain = `workflows/01-fashion-campaign-chain.md`, frames = `workflows/02-lookbook-shot-list.md`, post = `references/prompts-v2/campaign-finish-plan.md`. **STRUCTURE skill by design — fidelity MEDIUM on chain, LOW on craft (source paywalled, not purchased).** It carries the spine and hands off the frame: craft backfill is **mandatory**, not optional → St. Pierre (frame construction) · Flynn (board ops, volume consistency) · Clark (light, black point, flatness). Tool stack quarantined in `genius.md` Appendix A | Four checks. **(1) One sentence, one metaphor** — if the concept takes two sentences it's two campaigns, and everything downstream is decoration. **(2) Cover the captions and read the set** — one light logic, one palette, one person, one world, or it's six photographers and a folder. **(3) The hero object is accurate** — the model *will* render the product wrong; that's a budgeted compositing pass (product → subject → background), never a better prompt, and never grounds for killing a good generation. **(4) Could a crew have shot this** — location, hour, lens, a body that fits, fabric that behaves; most "looks AI" is a physics failure, not an aesthetic one. Then: **one** series grade, set-level, matched to the **brand's** palette — never per-frame. Grade the result against **`https://maisonmeta.io/work`** (Norma Kamali, YSL Montaigne, Tabi, Bottega Jodie), never against this skill — it is the process floor, not the ceiling |

| **Motion graphics / video overlay comps (Edit Bay)** — lower thirds, stat cards, title cards, diagram beats in HyperFrames/Remotion | `skills/video-studio/style/graphics-taste.md` § named load (its rules + `frontend-design` skill + style-file merge → `_active/farrice-brand/voice/video-style.md`; grace-liu on taste-fog) | Run graphics-taste's deterministic pre-render grep (off-brand fonts/hexes/shadow stacks); §10 slop tells are auto-blockers; the travel test — name three invisible decisions or it's mass-produced |

**Intent mirror (creative asks):** before generating from a raw dump, reflect back in ≤5 lines:
deliverable + format, felt standard ("cinematic macro, matte, no gloss"), references in play,
budget, and the ONE thing that would make it his ("what's the detail that makes this yours?").
Proceed on his confirm or visible non-objection; never silently guess taste on new ground.

**Show craft with the quote:** for paid video, the crafted prompt is presented WITH the cost
quote — one look approves money and craft together.

This map extends as masters are extracted (see the master-hunt mission). New master → new row.
