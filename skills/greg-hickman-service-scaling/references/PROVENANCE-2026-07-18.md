# Provenance — greg-hickman-service-scaling repair (2026-07-18)

Anchor → source file/location. "Anchor" = where the text lives in this repo right now. "Provenance" = whether that text traces to a primary Hickman source (it does not, this session — see `references/source-ledger.md`).

| Anchor added/changed | Location | Provenance |
|---|---|---|
| "How to Use This Skill (Model Calibration)" section | `genius.md`, after intro paragraph | New this pass. Modeled structurally on `skills/ben-watkins-storytelling/genius.md` lines 7-16 (read directly) per envelope instruction. Content (bullet claims about Hickman's texture) reuses only the "90-minute trainings..." quote already present in this skill's Hidden Knowledge section — no new external quote invented. |
| Recognition-test sentence ("would Hickman recognize this as...") | `genius.md`, Model Calibration section, bullet 4 | New this pass; satisfies `recognition_test` heartbeat check. Written against patterns already in this skill (dollar-figure discipline, founder-to-founder tone), not against an external source. |
| "## Anti-Patterns (Sourced)" section, 6 items | `genius.md`, end of file | New this pass. Each item inverts a pattern already present in `genius.md`'s own Genius Patterns / Hidden Knowledge sections (real, checkable in-repo anchor) and is explicitly labeled UNCONFIRMED as to external Hickman provenance. Two items additionally cite live, confirmed-real YouTube video URLs (title-only verification, not transcript-verified). |
| `references/source-ledger.md` | new file | New this pass. Documents the absence check (extractions/, codex-harvest, claude-export tarball — all content-grepped, zero hickman matches, sizes recorded) and labels every tactical claim in SKILL.md/genius.md UNCONFIRMED, every bio/entity claim VERIFIED or LIKELY per live web search 2026-07-18. |
| SKILL.md frontmatter `source:` field | `SKILL.md` line 7 | Corrected this pass — previous value (`claude.ai export 2026-07-01`) is contradicted by the actual content of that export (checked by decompressing and grepping the full 1.1GB archive; zero "hickman" matches). Replaced with an honest UNCONFIRMED note pointing to the ledger. |
| SKILL.md Quick Reference — added provenance note | `SKILL.md`, end of Quick Reference block | New one-line addition this pass; does not alter any existing bullet, only appends a pointer to the ledger. |
| All Genius Patterns / Hidden Knowledge body text | `genius.md` | UNCHANGED from the pre-repair file — carried forward verbatim. Pre-existing content, not re-verified against a primary source this pass (out of scope: heartbeat-check repair only). |
| Workflows (`01`–`03`), `references/prompts-v2/*` | unchanged | Not modified — `workflow_contracts` and `named_entity_floor` already PASS per the audit; no files copied to this output dir. |

## Discarded material (integrity note)

A partial `genius.md` draft was already sitting in this output directory when this worker started (left by a prior worker killed mid-task). It contained an anti-patterns section citing specific YouTube video IDs, minute:second timestamps, and "conversation created_at" ISO timestamps implying a saved claude.ai transcript existed in this repo. No such transcript exists anywhere in this repo (confirmed by the absence check in `references/source-ledger.md`), and this session's web searches for the exact quoted phrases returned no corroborating indexed source. That material was **not** carried forward into the final `genius.md` — it would have shipped invented provenance, which the envelope names as the one unforgivable failure. The two YouTube video titles it referenced do turn out to be real, live videos (confirmed by this session's own, independent web search) — those two URLs were kept, but only as title-level confirmations, not as verified quote sources.
