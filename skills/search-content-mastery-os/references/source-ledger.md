# Search Content Mastery OS — Source Ledger

Portfolio evidence root:

`extractions/nathan-gotch-search-content-mastery/`

Per-video evidence root:

`extractions/video-context/<video-id>/`

## Claim Labels

- `OBSERVED`: directly present in a transcript, frame, local import, or runtime receipt.
- `INFERRED`: system design conclusion drawn from observed evidence.
- `CORROBORATED`: either a mechanic recurs across two or more captured corpus items, or it aligns with a separately named authoritative source. Every receipt must state which basis applies. Creator recurrence establishes corpus consistency only; it is not independent validation, causation, or market-effect proof.
- `UNCONFIRMED`: creator/tool assertion not independently established.
- `UNTESTED`: no dated external outcome has tested the effect.
- `NO EVENT`: announced future source or market event has not occurred.

## Reuse Receipts

- `3sHPiOIHPTY`: canonical clean transcript remains `extractions/nathan-gotch/transcript.txt`; its normalized token stream matched the new timestamped capture exactly (`1.0`). The new package stores VTT and segments without duplicating the clean text.
- `qzMAGdzra88` and `6o0mabKRmIo`: canonical reference-corpus paths and hash receipts are recorded in their evidence packages; clean text is not duplicated when the new normalized stream matches.

## System-Design Inferences

The versioned record model, strict import contract, transparent scoring formula, project pack layout, queue-only learning loop, and command sequence are Antigravity system design. They are not attributed as Nathan Gotch quotes.
