# Arsenal Routing — Routes Out (thin orchestration)

This OS owns only the net-new gaps: zero-point identity formation, true-fan density, generalized founder intake, the Five Identity Drivers, homie-lookbook, the top-down/bottom-up control fork, the aspiration-gap split, the two-demographic split, weak-point resolution, and the mirror-test ethics gate. **Everything else routes out.** Do not re-teach any owned capability inside this skill's workflows or genius context.

Cross-hub handoffs are options a composer reaches for, never forced pipeline steps.

---

## Route-out table

| Condition (what the user actually needs) | Route to |
|---|---|
| Select the creator **content archetype** (Oracle / Performer / World Builder / Catalyst / Helper) and build the content roadmap | `oren-brand-archetypes` (front door) — `/oren-archetype-diagnostic` |
| Resource-reality audit for archetype eligibility (who's on camera, who designs, budget) | `oren-brand-archetypes` — `/oren-resource-audit` |
| "Our industry is boring" objection | `oren-brand-archetypes` — `/oren-boring-industry-excavation` |
| Scale a **team / pods / hiring / operating cadence** once traction exists | `oren-content-team-architecture` |
| Founder content identity, creator networks, brand-as-media-company, paid/organic bridge at scale | `oren-content-team-architecture` |
| **Premium / luxury positioning** once the identity has value; insider codes, connoisseurship ladders, aesthetic world-building | `oren-luxury-psychology` |
| **Reposition** a generic brand/creator to differentiated; category-code audit, counterpositioning, vision extension into "worlds" | `oren-repositioning` |
| **Taste QC** / creative-judgment gate (CEV critique, is-this-good) | `oren-taste-development` |
| **Ops infrastructure** once producing: reference repos, ideas→calendar, weekly updates, team trackers, process docs | `oren-operational-systems` |
| **Solo-operator execution engine** (run a team's output as one person with AI): weekly OS, MESSAGES cycle, INFO-RELEASE/AEO, referral engine, virality audit | `oren-one-person-ai-marketer` |
| **AI-clone / identity-capture / brand-voice machine** (a persistent voice Project or full system-prompt clone) — OUT OF SCOPE HERE | `oren-one-person-ai-marketer` → `oren-brand-voice-machine` |
| Grounded **ICP / avatar research** (cold-start → converting-copy avatar manifold) | `avatar-machine` (Luke Iha) |
| Find the ignored **sub-identity inside a saturated niche** (sharpens Subculture-pride) | `meg-sub-identity-map` (Meg Heckman) |
| **Campaign-level identity naming** (name the identity in one word) validated against consumer posture | `lynch-identity-campaign` |
| **Farrice's own voice** on any output | `voice-os` (BLEND default), layered before the content workflow |
| Full **6-layer Brand Operating System** document set once identity is locked | `build-bos` |
| **Ad-testing / cold-start converting copy** downstream of the avatar or demo split | `dara-*` / `copy-engine` |
| Sales-conversation identity shifting (transform a prospect's self-concept mid-conversation) | `michael-bernoff-identity-engineering` / `jeremy-miner-identity-persuasion` |
| **1,000-true-fans unit economics** grounding | Kevin Kelly, "1,000 True Fans" (2008) — principle, not paid tool |

---

## Future source-hunt (flagged, not reinvented)

The deeper **AI Clone Architecture Builder** (Oren John "Crown Jewel" prompt, 1 of 7 captured in Recall, card `71602c2a`) builds a full system-prompt clone from a content archive, deeper than the existing `oren-brand-voice-machine`. It was never fully extracted (only 1 of 7 prompts surfaced). Treated as a **future source-hunt**, not reinvented shallower here. If a user needs AI-clone depth today, route to the existing lighter `oren-brand-voice-machine` and flag the deeper version as pending extraction.
