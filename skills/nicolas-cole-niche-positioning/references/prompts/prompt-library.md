# Prompt Library Pointer

The maintained structure-pure prompt contracts live in [`../prompts-v2/`](../prompts-v2/). This pointer preserves compatibility with the legacy validator without creating a second prompt implementation.
