# Source Ledger — product-design-build

Every sourced claim in `genius.md` and the `workflows/*.md` Quality Gates, claim-by-claim. This is a method/tool skill (DESIGN.md → UI codegen) — there is no single "expert transcript" to ground against; ground truth is the technical documentation for the libraries and standards this skill wires together (CVA, Radix, Tailwind, WCAG, axe-core), plus the skill's own prior files. Labels: **VERIFIED** (fetched and quote confirmed in the live source, 2026-07-18) / **LIKELY** (established, widely-documented technical fact, not independently re-fetched this pass) / **UNCONFIRMED** (no source found; flagged, not anchored).

- **class-variance-authority (CVA) — "Class Variance Authority," typed variant API library.** VERIFIED — fetched `github.com/joe-bell/cva` 2026-07-18; repo description confirms the name expansion and purpose (typed variants over plain className strings).
- **Radix Dialog — focus trap, Escape-to-close, focus restoration to `Dialog.Trigger`.** VERIFIED — fetched `radix-ui.com/primitives/docs/components/dialog` 2026-07-18; documentation confirms "Focus is automatically trapped within modal," "Esc closes the component automatically," and that Esc "moves focus to Dialog.Trigger."
- **WCAG 2.2 Success Criterion 1.4.11 (Non-Text Contrast) — 3:1 contrast ratio for UI components and states.** VERIFIED — fetched `w3.org/WAI/WCAG21/Understanding/non-text-contrast.html` 2026-07-18; page confirms the exact 3:1 threshold and "Visual information required to identify user interface components and states" scope.
- **Tailwind `hover:` → `@media (hover: hover)`; `pointer-coarse:` for touch.** VERIFIED — fetched `tailwindcss.com/docs/hover-focus-and-other-states` 2026-07-18; page confirms the `pointer` media-query variants and the coarse-pointer (touchscreen) targeting language.
- **axe-core — "Accessibility engine for automated Web UI testing."** VERIFIED — fetched `github.com/dequelabs/axe-core` 2026-07-18; repo description quoted verbatim.
- **`workflows/03-preview-iterate.md` anti-pattern — "code reading is unreliable for visual work; always render."** VERIFIED — verbatim string present in this skill's own `workflows/03-preview-iterate.md` (Anti-patterns section, unchanged by this repair pass).
- **`references/prompts-v2/component-build.md` frontmatter — `refactored: 2026-07-13`.** VERIFIED — read directly from this skill's own file frontmatter.
- **`rounded.md` renders as 8px "in Heritage example."** VERIFIED — verbatim phrase already present in this skill's own `workflows/03-preview-iterate.md` critique table (unchanged by this repair pass); the "Heritage example" DESIGN.md itself is not in this repo, so the specific 8px value is this skill's own prior internal claim, not independently re-verified against an external Heritage brand spec — carried forward as-is, not upgraded.
- **`class-variance-authority` vs. styled-components/Emotion (runtime cost) and tw-classed (TypeScript ergonomics).** LIKELY — standard, widely-documented tradeoffs in the CSS-in-JS vs. utility-class ecosystem; not independently re-fetched from a single primary source this pass (pre-existing claim, carried forward from the prior genius.md).
- **Tailwind v4's CSS-first `@theme` block compatibility with `theme.extend`-style token wiring.** LIKELY — consistent with Tailwind's own v4 migration documentation at a high level; not fetched verbatim this pass, so held at LIKELY rather than VERIFIED.
- **Google Labs' `@google/design.md` CLI (`lint`, `export --format tailwind|dtcg`).** UNCONFIRMED as an external, independently-verifiable package — no `extractions/` source and no live fetch performed against an npm/GitHub listing for `@google/design.md` in this pass. Treated throughout `genius.md` and `workflows/*.md` as this skill's own established internal convention (present before this repair, unchanged), not as a newly sourced external claim.
- **Style Dictionary transform groups (`ios-swift`) for token → Swift constant generation.** LIKELY — matches Style Dictionary's publicly documented `ios-swift` transform group naming; not re-fetched this pass.

## What was NOT sourced (honest gaps)
- The specific hex values in the Token → Tailwind Mapping example (`#1A1C1E`, `#6C7278`, etc.) are illustrative sample tokens carried over from the pre-existing file, not drawn from a real, named brand's DESIGN.md. They were already present before this repair pass and are left as an illustrative example, not represented as sourced fact anywhere in the text.
- No `extractions/` directory exists for this skill (it is a method/tool skill, not a persona extraction) — confirmed by directory search 2026-07-18 (`extractions/design-thinking-src` contains 5 unrelated video-transcript folders, `extractions/rafa-conde` and `extractions/mark-kashef-visual-design` are separate personas, neither is DESIGN.md-specific). This ledger's anchors are therefore live technical documentation, not a local transcript.
