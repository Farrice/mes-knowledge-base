---
description: "/jen — the ONE front door for Jen Santulan (@_jiing) content. Nine steps in a fixed order, a receipt written into the week folder after each, no step skipped silently: LOAD → READ → RESEARCH → WRITE → AMPLIFY → CHECK → RENDER → DELIVER → LEARN. The run folder is the state (run.yaml + pipeline-log.md, 2026-09-09). Operating model: _active/clients/jen-listings/06-system/ENGINE-V2.md. Replaces /jen-engine, "
---
<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

Read and execute the workflow at `.agent/workflows/jen.md` - /jen — the ONE front door for Jen Santulan (@_jiing) content. Nine steps in a fixed order, a receipt written into the week folder after each, no step skipped silently: LOAD → READ → RESEARCH → WRITE → AMPLIFY → CHECK → RENDER → DELIVER → LEARN. The run folder is the state (run.yaml + pipeline-log.md, 2026-09-09). Operating model: _active/clients/jen-listings/06-system/ENGINE-V2.md. Replaces /jen-engine, 
