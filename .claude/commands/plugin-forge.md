---
description: "/plugin-forge — /forge plugin <skill / prompt-set / workflow family> — Plugin Forge lane: a proven asset family → an installable Claude Code plugin. HARD-GATED: builds nothing without a verbatim operator lift token + 4/4 lift-plan fixtures passing in one run."
---
<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

Read and execute the workflow at `.agent/workflows/plugin-forge.md` - /plugin-forge — /forge plugin <skill / prompt-set / workflow family> — Plugin Forge lane: a proven asset family → an installable Claude Code plugin. HARD-GATED: builds nothing without a verbatim operator lift token + 4/4 lift-plan fixtures passing in one run.
