---
description: "/ctm-refresh — Phase 6 freshness — keep the map a living document with a quarterly light pass + 1–2× yearly deep rebuild, add new word-for-word quotes, flag new phrasing/worries/wishes, write a dated change-log at the top (which becomes its own asset), and optionally register a recurring refresh job via /schedule"
---
<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

Read and execute the workflow at `.agent/workflows/ctm-refresh.md` - /ctm-refresh — Phase 6 freshness — keep the map a living document with a quarterly light pass + 1–2× yearly deep rebuild, add new word-for-word quotes, flag new phrasing/worries/wishes, write a dated change-log at the top (which becomes its own asset), and optionally register a recurring refresh job via /schedule
