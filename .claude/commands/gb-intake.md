---
description: "/gb-intake — Client intake → free personalized mini-report inside the 48h promise: reads the frozen-shape intake-pack, seeds the radar from their own answers, bakes the mini, stages a Gmail DRAFT (never sends). Paid chain fires only on Farrice's explicit call."
---
<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

Read and execute the workflow at `.agent/workflows/gb-intake.md` - /gb-intake — Client intake → free personalized mini-report inside the 48h promise: reads the frozen-shape intake-pack, seeds the radar from their own answers, bakes the mini, stages a Gmail DRAFT (never sends). Paid chain fires only on Farrice's explicit call.
