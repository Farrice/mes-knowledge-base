---
description: "/hg-judgment-encode — The crown-jewel pipeline — turn an expert's tacit taste into a deployable evaluator tool via edit-pair mining (Column A/B → patterns → criteria → plain-English pass/fail rubric → system prompt)"
---
<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

Read and execute the workflow at `.agent/workflows/hg-judgment-encode.md` - /hg-judgment-encode — The crown-jewel pipeline — turn an expert's tacit taste into a deployable evaluator tool via edit-pair mining (Column A/B → patterns → criteria → plain-English pass/fail rubric → system prompt)
