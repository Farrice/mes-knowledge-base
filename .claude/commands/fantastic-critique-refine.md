---
description: "Critique each render on Virgil \u00d7 LIFT \u00d7 type \u00d7 anti-slop, then make targeted --mask edits and re-render \u2014 the loop that makes it remarkable"
---

Read and execute the workflow at `.agent/workflows/fantastic-critique-refine.md` — Critique each render on Virgil × LIFT × type × anti-slop, then make targeted --mask edits and re-render — the loop that makes it remarkable

ARGUMENTS: $ARGUMENTS
