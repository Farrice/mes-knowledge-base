---
description: "Atomize material into chunks, physically cluster them into themes until the structure reveals itself, then engineer the connective tissue between thematic blocks so the piece reads as one yarn."
---

Read and execute the workflow at `.agent/workflows/orlean-card-structure.md` — Atomize material into chunks, physically cluster them into themes until the structure reveals itself, then engineer the connective tissue between thematic blocks so the piece reads as one yarn.

ARGUMENTS: $ARGUMENTS
