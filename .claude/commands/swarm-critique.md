---
description: "/swarm-critique — the metered critique + research swarm (both harnesses). One pen writes; ≤4 read-only seats cast from the skill library; NAMED bar; Farrice judges blind; $10 hard stop with a receipt. Policy: directives/swarm-usage-policy.md."
---
<!-- hand-written shim (2026-09-09), same shape as the minted wrappers; safe to delete when the parity sweep mints it -->

Read and execute the workflow at `.agent/workflows/swarm-critique.md` - /swarm-critique — router → one pen → parallel seats → blind judge → receipt. Print every receipt line; a nonzero `swarm_meter.py price` is a stop. Usage: `/swarm-critique <artifact-path> --bar <bar-path> [--lenses a,b,c,d] [--dial lightweight|full]`.
