---
description: "Generate working UI components and pages from a DESIGN.md — the forward-generation half of the design-systems-as-code stack. Takes a validated DESIGN.md plus a component or page spec and produces React+Tailwind, Vue, or SwiftUI code that compiles, renders, and matches the bran..."
---
<!-- auto-generated: skill-command shim (sync_registries.py) — safe to delete; regenerated on sync -->

Load and embody the skill at `skills/product-design-build/SKILL.md`. Also load `skills/product-design-build/genius.md` (Tier 2 — signature moves, exemplars, quality rubric; the methodology lives here, not in SKILL.md). Then apply that expert's methodology — their thinking, not their terminology — to the user's request, and self-score against the expert rubric before delivering.
