---
description: "Pin the current work session \u2014 stamp a Session title into today's briefing (or --file) and drop a thread pointer so /resume surfaces it by name. Manual lever for the auto-pin formula (chain_runner.finalize + /end-session + the Stop-hook bac"
---

Read and execute the workflow at `.agent/workflows/pin-session.md` — Pin the current work session — stamp a Session title into today's briefing (or --file) and drop a thread pointer so /resume surfaces it by name. Manual lever for the auto-pin formula (chain_runner.finalize + /end-session + the Stop-hook bac

ARGUMENTS: $ARGUMENTS
