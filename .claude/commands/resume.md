---
description: "Resume any past work-thread from the durable handoff store — menu, or /resume <thread|keyword|number>. You choose which, not just the last one."
---

Read and execute the workflow at `.agent/workflows/resume.md` — Multi-thread session resume. ARGUMENTS (if present) = the thread selector (number, name, or keyword) → resume that thread (Step 2). No arguments → show the triage menu and WAIT for the user to pick (Step 1).
