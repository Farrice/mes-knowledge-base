---
name: "source-command-kling-multishot"
description: "/kling-multishot — Kling v3 Pro accepts a `multi_prompt` array — each entry defines one shot's prompt, and Kling distributes the total duration across them, generating natural cuts between shots. Unlike Seedance (single-shot only), this lets you tell a 3-act story in one generation."
---

# source-command-kling-multishot

Use this skill when the user asks to run the migrated source command `kling-multishot`.

## Command Template

<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

Read and execute the workflow at `.agent/workflows/kling-multishot.md` - /kling-multishot — Kling v3 Pro accepts a `multi_prompt` array — each entry defines one shot's prompt, and Kling distributes the total duration across them, generating natural cuts between shots. Unlike Seedance (single-shot only), this lets you tell a 3-act story in one generation.
