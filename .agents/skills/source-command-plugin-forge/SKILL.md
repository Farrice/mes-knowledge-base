---
name: "source-command-plugin-forge"
description: "/plugin-forge — /forge plugin <skill / prompt-set / workflow family> — Plugin Forge lane: a proven asset family → an installable Codex plugin. HARD-GATED: builds nothing without a verbatim operator lift token + 4/4 lift-plan fixtures passing in one run."
---

# source-command-plugin-forge

Use this skill when the user asks to run the migrated source command `plugin-forge`.

## Command Template

<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

Read and execute the workflow at `.agent/workflows/plugin-forge.md` - /plugin-forge — /forge plugin <skill / prompt-set / workflow family> — Plugin Forge lane: a proven asset family → an installable Codex plugin. HARD-GATED: builds nothing without a verbatim operator lift token + 4/4 lift-plan fixtures passing in one run.
