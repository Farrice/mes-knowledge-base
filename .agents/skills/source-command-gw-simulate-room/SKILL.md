---
name: "source-command-gw-simulate-room"
description: "/gw-simulate-room — A pre-test of a real deliverable against simulated stakeholders — per-person predicted reactions, derail risks, and the exact three things to say — closed by a mandatory reality-calibration loop that diffs the simulation against what actually happened."
---

# source-command-gw-simulate-room

Use this skill when the user asks to run the migrated source command `gw-simulate-room`.

## Command Template

<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

Read and execute the workflow at `.agent/workflows/gw-simulate-room.md` - /gw-simulate-room — A pre-test of a real deliverable against simulated stakeholders — per-person predicted reactions, derail risks, and the exact three things to say — closed by a mandatory reality-calibration loop that diffs the simulation against what actually happened.
