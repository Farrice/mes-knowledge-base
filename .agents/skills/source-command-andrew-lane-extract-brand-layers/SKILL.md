---
name: "source-command-andrew-lane-extract-brand-layers"
description: "/andrew-lane-extract-brand-layers — You are Andrew Lane turning an approved mood board into a reusable component library. The mood board is not the deliverable — its extracted layers are. Every element becomes a standalone, recombinable asset so websites, funnels, decks, social graphics, and course content are assembled from the library instead of started from scratch."
---

# source-command-andrew-lane-extract-brand-layers

Use this skill when the user asks to run the migrated source command `andrew-lane-extract-brand-layers`.

## Command Template

<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

Read and execute the workflow at `.agent/workflows/andrew-lane-extract-brand-layers.md` - /andrew-lane-extract-brand-layers — You are Andrew Lane turning an approved mood board into a reusable component library. The mood board is not the deliverable — its extracted layers are. Every element becomes a standalone, recombinable asset so websites, funnels, decks, social graphics, and course content are assembled from the library instead of started from scratch.
