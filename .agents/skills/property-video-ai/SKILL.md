---
name: property-video-ai
description: Create or resume cinematic property films from listing photos or URLs using the locally installed source-grounded workflow and approved specimens. Shares one private runtime between Codex and Claude Code.
---
# Cinematic property film — shared private runtime

Resolve this SKILL.md symlink to its real path. The repository root is four levels above that file (the parent of `.agents`). Run `python3 <repository>/execution/property_video_runtime.py status` and read the returned `approved_standard` and `primary_workflow` before production. Run the same helper with `verify` before reusing approved assets.

The runtime lives at `<repository>/.private/property-video/`. Read CURRENT.json there. The runtime's `runtime/integration-map.json` relocates the original task paths; use it instead of stale absolute paths in preserved historical source maps. Source packages, exact prompts, media and receipts stay private and are not contained in this public bridge.

Follow the approved primary specimen and the user's current task. Preserve earlier approved films as history. Silent output is the default. Read the shared budget ledger, reconcile live use and other active tasks, and apply the current spending approval before any generation. Installation is not a new credit allowance. Do not automatically invoke FAL or buy Higgsfield. Inspect the actual source frame and load the camera-motion recipe before generating.

Keep single-scene orbit, motion-matched editing and anchored start/end transitions distinct. A connected editorial sequence does not prove a physically continuous room-to-room camera move. Preserve architectural fidelity and reject invented geometry. Deliver individual silent clips and an editable recipe with the film.

For technical operation, use the installed workflow's local renderer and resumable-state tools. Archived paths are evidence, not new output destinations. New projects belong under the established client home in an authoring lane. If the private runtime is absent, report that exact installation gap; do not download or expose the paid course files through a public repository. This bridge is installed globally by symlink so both harnesses read the same current instructions.

For preserved Armida replays, use the relocated `runtime/state-wide.json`, `runtime/state-clean.json` and `runtime/state-portrait.json`, verified in `runtime/REPLAY-RECEIPT.json`. Original source states remain immutable historical evidence. Keep the original approval scope: the wide marketing film is approved; companions have no separate taste approval.
