---
name: "source-command-cost-per-right-answer"
description: "/cost-per-right-answer — Define the exact question and what a wrong answer costs. Read `references/research-tool-contract.md` and separate `PRIVATE_CONTEXT` from the minimum `PUBLIC_QUERY`. Paid or quota-heavy tools require an approved cost gate. If answer correctness cannot be evaluated, redesign the question first."
---

# source-command-cost-per-right-answer

Use this skill when the user asks to run the migrated source command `cost-per-right-answer`.

## Command Template

<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

Read and execute the workflow at `.agent/workflows/cost-per-right-answer.md` - /cost-per-right-answer — Define the exact question and what a wrong answer costs. Read `references/research-tool-contract.md` and separate `PRIVATE_CONTEXT` from the minimum `PUBLIC_QUERY`. Paid or quota-heavy tools require an approved cost gate. If answer correctness cannot be evaluated, redesign the question first.
