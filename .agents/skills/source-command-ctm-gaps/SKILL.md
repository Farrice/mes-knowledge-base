---
name: "source-command-ctm-gaps"
description: "/ctm-gaps — Phase 4 Step 2 engine — build the gap table (Pain/Job → Current Fix → The Gap → Gap-Width 1–5), sourcing Current Fix from competitor products + the map's ⚠ WORKAROUND DIY fixes, then sort descending by gap width. The widest-gap rows are the named shortlist passed to /ctm-to-copy, /ctm-to-content, /ctm-to-offer"
---

# source-command-ctm-gaps

Use this skill when the user asks to run the migrated source command `ctm-gaps`.

## Command Template

<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

Read and execute the workflow at `.agent/workflows/ctm-gaps.md` - /ctm-gaps — Phase 4 Step 2 engine — build the gap table (Pain/Job → Current Fix → The Gap → Gap-Width 1–5), sourcing Current Fix from competitor products + the map's ⚠ WORKAROUND DIY fixes, then sort descending by gap width. The widest-gap rows are the named shortlist passed to /ctm-to-copy, /ctm-to-content, /ctm-to-offer
