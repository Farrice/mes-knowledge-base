---
name: "source-command-hook-trust-stack"
description: "/hook-trust-stack — Stacking workflow: Kallaway's lock-in trust levers × Luke Iha's proof mechanisms. For assets where the hook must not just stop the scroll but open a wallet — the trust anchor becomes a proof ladder."
---

# source-command-hook-trust-stack

Use this skill when the user asks to run the migrated source command `hook-trust-stack`.

## Command Template

<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

Read and execute the workflow at `.agent/workflows/hook-trust-stack.md` - /hook-trust-stack — Stacking workflow: Kallaway's lock-in trust levers × Luke Iha's proof mechanisms. For assets where the hook must not just stop the scroll but open a wallet — the trust anchor becomes a proof ladder.
