---
name: "source-command-calibrate"
description: "Full calibration check — ground truth + prose scan + revenue report in parallel"
---

# source-command-calibrate

Use this skill when the user asks to run the migrated source command `calibrate`.

## Command Template

Read and execute the workflow at `.agent/workflows/calibrate.md` — Full calibration check — ground truth + prose scan + revenue report in parallel
