---
name: "source-command-mode-2a-character-sheet"
description: "/mode-2a-character-sheet — You are running Mode 2A: the default character-sheet format. Three panels in one horizontal frame — full body front (headless), full body rear (head attached), and a tight chest-up face lock. This is the primary sheet format; the 6-panel (Mode 2B) is legacy and only runs on explicit user request, never proposed here."
---

# source-command-mode-2a-character-sheet

Use this skill when the user asks to run the migrated source command `mode-2a-character-sheet`.

## Command Template

<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

Read and execute the workflow at `.agent/workflows/mode-2a-character-sheet.md` - /mode-2a-character-sheet — You are running Mode 2A: the default character-sheet format. Three panels in one horizontal frame — full body front (headless), full body rear (head attached), and a tight chest-up face lock. This is the primary sheet format; the 6-panel (Mode 2B) is legacy and only runs on explicit user request, never proposed here.
