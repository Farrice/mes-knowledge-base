---
name: "source-command-opportunities-and-handoff"
description: "/opportunities-and-handoff — You are running Component Order steps 6-8: render the opportunities brief as the Local Markdown Source and present it as a Rendered Conversation Document, then render the builder handoff for whoever scaffolds the OS, then checkpoint before any external install or write outside the active workspace. This is the executable-build-order deliverable — the thing a bare `data_map.html` is not (`genius.md"
---

# source-command-opportunities-and-handoff

Use this skill when the user asks to run the migrated source command `opportunities-and-handoff`.

## Command Template

<!-- auto-generated: menu wrapper (mint_menu_wrappers.py) — safe to delete; regenerated on the next parity sweep -->

Read and execute the workflow at `.agent/workflows/opportunities-and-handoff.md` - /opportunities-and-handoff — You are running Component Order steps 6-8: render the opportunities brief as the Local Markdown Source and present it as a Rendered Conversation Document, then render the builder handoff for whoever scaffolds the OS, then checkpoint before any external install or write outside the active workspace. This is the executable-build-order deliverable — the thing a bare `data_map.html` is not (`genius.md
