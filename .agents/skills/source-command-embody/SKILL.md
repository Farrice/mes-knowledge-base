---
name: "source-command-embody"
description: "Explicit-only /embody command. Practice one judgment from an extraction, artifact, or current session; never select implicitly."
---

# source-command-embody

Use only when the user explicitly invokes `/embody` or `$source-command-embody`.

Read and execute `.agent/workflows/embody.md`. The canonical owner is
`/operator-school`; this wrapper must remain a thin command bridge and must not
create a competing teaching system.
